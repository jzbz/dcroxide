# Parity ledger

Maps each dcrd package to its dcroxide crate and tracks parity status against
the pinned tag **`release-v2.1.5`** (wire protocol 11, JSON-RPC 8.3.0; the
project brief says protocol 12, but the source at the pinned tag says 11 and
source wins).

Status legend:

- **—** not started
- **WIP** implementation in progress
- **vectors** dcrd test vectors ported or regenerated via `tools/oracle`
- **diff** live differential coverage against the dcrd oracle
- **parity** vectors + differential + fuzz sign-off complete

| dcrd package | dcroxide crate | Status | Notes |
|---|---|---|---|
| `crypto/blake256` | `dcroxide-crypto` | vectors + diff | Vendored from dcr-rs `fd32c1a`; KATs regenerated via oracle; live differential test + incremental-hashing fuzz target |
| `crypto/ripemd160` | `dcroxide-crypto` | vectors | RustCrypto `ripemd` one-shot digest (dcrd's package is itself the old x/crypto impl); standard Bosselaers reference vectors pinned. Consumed by `txscript` OP_RIPEMD160/OP_HASH160 |
| `crypto/rand` | `dcroxide-crypto` | — | Phase 1 |
| `chaincfg/chainhash` | `dcroxide-chainhash` | vectors + diff | `hash_test.go` vectors ported (incl. short-string zero-pad quirk); parse/display differential + fuzz target. Not ported: Go-specific plumbing (`SetBytes` pointer API, marshalers) |
| `dcrec/secp256k1` + `ecdsa` (type 0) | `dcroxide-dcrec` | vectors + diff | dcrd's exact DER + pubkey acceptance (all 25 error kinds, incl. hybrid keys) over libsecp256k1 per ADR-0006; TestSignatureParsing/TestParsePubKey/TestSignatureSerialize ported; differential: parse verdict+kind+values, RFC6979 sign byte-equality, verify verdicts incl. high-S; 2 fuzz targets. Since ported for the netsync/RPC phases: compact-sig recovery (`RecoverCompact`, behind the `recovery` feature, driving the `verifymessage` handler).  Not ported: `SignCompact` (only wallets sign compact messages); `PrivKeyFromBytes` mod-N reduction (we reject out-of-range keys instead — not an observable surface) |
| `dcrec/secp256k1/schnorr` (type 2) | `dcroxide-dcrec` | vectors + diff | EC-Schnorr-DCRv0 on k256 per ADR-0006; dcrd's `NonceRFC6979` ported exactly (raw-key HMAC variant, extra-data/version/iteration semantics) and pinned by dcrd's own nonce vectors; TestSchnorrSignAndVerify ported (RFC6979 + explicit-nonce rows); differential: sign byte-equality, verify verdicts, parse verdict+kind; fuzz target. Unreachable-by-construction kinds (`ErrPrivateKeyIsZero`, `ErrPubKeyNotOnCurve`) not represented |
| `dcrec/edwards` (type 1) | `dcroxide-dcrec` | vectors + diff | Ed25519 on curve25519-dalek with dcrd's exact (2017-agl) acceptance implemented explicitly: sig-R parse accepts non-canonical encodings incl. x=0/sign-bit; pubkey parse rejects x=0/sign-bit (the `X >= P` quirk) and re-serializes canonically; raw verify checks only S's top 3 bits (full `s < L` at parse, the consensus path). RFC8032 TEST 1 KAT; differential: sign+pubkey byte-equality, edge-biased parse verdicts, raw-verify verdicts incl. s+L malleation; fuzz target. `sign_with_pub_key_bytes` reproduces dcrd's `PrivKeyFromBytes` signing path where the embedded (last-32-bytes) public key participates in the commitment (used by `txscript/sign`). Not implemented: scalar-based keys (`PrivKeyFromScalar`/`SignFromScalar`, wallet-side legacy) and threshold signing |
| `math/uint256` | `dcroxide-uint256` | vectors + diff | Full API port (arithmetic incl. Knuth 4.3.1D division, u64 variants, bitwise/shifts, comparisons, BE/LE bytes, text in bases 2/8/10/16) with dcrd's wrap-around and panic-on-zero-divisor semantics; differential across all 22 ops with boundary-biased operands; u128-reference + algebraic property tests; fuzz target. Not ported: `big.Int` interop (Go-specific), `fmt.Formatter` (Rust fmt traits provided; `text` is the parity surface) |
| `wire` | `dcroxide-wire` | vectors + diff | Framing (24-byte header, BLAKE-256 checksum, dcrd's exact validation order + error kinds) and **all 40 message types** at protocol 11 (39 decodable; reject is write-only per QK-0001): version (optional-field semantics), addr/getaddr, inv/getdata/notfound, getblocks/getheaders/headers/block, ping/pong/mempool, miningstate/getminingstate, sendheaders/feefilter, deprecated cf family + cfilterv2 + batched cfiltersv2, initstate/getinitstate, write-only reject (QK-0001), and the eight StakeShuffle mixing messages (`MsgMixPairReq`/`KeyExchange`/`Ciphertexts`/`SlotReserve`/`FactoredPoly`/`DCNet`/`Confirm`/`Secrets`) + `MixVect`. Frame differential: every message type × structured rounds byte-identical across all 4 networks + mutation verdict/kind parity; 3 fuzz targets + proptest laws. Also ported: the mixing identity hashes and signed-data preimages (`WriteHash`/`WriteSignedData`) as `mix_hash`/`signed_data` on each mix message, pinned through the mixing crate's vectors (dcrd silently hashes a partial preimage for locally-constructed invalid messages; the port surfaces the encode error instead — unreachable through decoding).  Not ported: `PowHashV2` (BLAKE3, with standalone crate). Note: the in-tree `wire` at the pinned tag says `ProtocolVersion` 11 while dcrd's `go.mod` pins the published `wire` v1.7.5 module, which defines 12 (`AddrV2Version`) plus the addrv2/netaddressv2 message family; dcrd 2.1.5 itself caps every use at `BatchedCFiltersV2Version` (11) — `server.go`'s `maxProtocolVersion` and the peer module's `MaxProtocolVersion` alike — so version 12 and addrv2 are never negotiated and the port pins 11 as the operative protocol surface |
| `chaincfg` | `dcroxide-chaincfg` | vectors + diff | All four networks' `Params` ported field-for-field: genesis blocks built from the wire types (reproducing testnet's `TxHash`-instead-of-`TxHashFull` merkle root and simnet's zero-instead-of-null-sentinel genesis input witness), canonical agenda vote definitions shared across per-network deployment schedules, block-one ledgers extracted mechanically from `subsidytables.go` (mainnet: 3,146 payouts / 1,680,000 DCR). Parity pinned two ways: dcrd's own serialized-genesis test vectors + genesis hashes, and a canonical full-params dump compared byte-for-byte against dcrd's `chaincfg/v3 v3.3.0` via the oracle across all networks (every field, every deployment/vote/choice string, serialized genesis, BLAKE-256 ledger commitment). dcrd's init-time deployment validation (`init.go`) ported as data sanity tests. Deliberate divergence: the mainnet `Seeders` list carries `dcr-seed.jz.bz` in addition to dcrd's three seeders (the oracle differential asserts its presence and excludes it from the byte-for-byte dump comparison).  Not ported: deprecated `Checkpoints` (no network sets it), trivial Go-interface getter methods (fields are public), `DNSSeed.String` |
| `txscript` (engine) | `dcroxide-txscript` | vectors + diff | The version-0 script engine ported from `txscript/v4 v4.1.2`: allocation-free tokenizer (exact `ErrMalformedPush` semantics), `ScriptNum` with minimal-encoding rules, the full 256-opcode table (generated from dcrd's `opcodeArray` and machine-verified entry-for-entry, incl. Decred's stake/treasury opcodes and the consensus-frozen `OP_SUBSTR`/rotation/shift quirks), the engine with all seven flags, P2SH (incl. stake-tagged) handling, conditional-execution tracking and clean-stack/push-only rules, split prefix/witness sighash under BLAKE-256, DER/pubkey/hashtype strict-encoding checks, `CHECKSIG`/`CHECKMULTISIG`/`CHECKSIGALT` across all three signature suites via `dcroxide-dcrec`, and the `ScriptBuilder`. Every error carries an `ErrorKind` whose `kind_name` matches dcrd's. Parity: dcrd's entire `script_tests.json` (2,285 rows), `tx_valid`/`tx_invalid`/`sighash.json` corpora ported verbatim, plus a live differential fuzzer (random scripts × random flags × versions, verdict+kind) and a sighash byte-equality differential over random txs; `txscript_engine` fuzz target. Not ported: `SigCache` (a result-invariant concurrency optimization) and the permanently-disabled `optimizeSigVerification` prefix cache (dead code at the tag) |
| `txscript/stdaddr`, `txscript/stdscript` | `dcroxide-txscript` (`stdaddr`/`stdscript` modules) | vectors + diff | All seven version-0 address kinds (P2PK/P2PKH across ECDSA-secp256k1, Ed25519, and Schnorr-secp256k1, plus P2SH) as one enum mirroring dcrd's interface set (stake methods `None` for non-`StakeAddress` kinds); decode with dcrd's exact error kinds; payment plus all six stake/treasury script constructors including the log2-encoded commitment fee limits. Full version-0 standard-script classification/extraction (21 script types with dcrd's names), multisig details, atomic-swap template extraction, `MultiSigScriptV0`/`ProvablyPruneableScriptV0`. Differential: every generated address kind round-tripped and dumped across its full observable surface byte-for-byte vs dcrd across all 4 networks (incl. boundary-biased commitment fee limits), corrupted/random-string decode kind parity, and classification/extraction/required-sigs parity over standard templates, mutations, and structured random scripts |
| `txscript/sign` | `dcroxide-txscript` (`sign` module) | vectors + diff | `RawTxInSignature`/`SignatureScript` across all three suites (secp256k1 keys 32 bytes, Ed25519 the 64-byte `seed‖pubkey` form dcrd's `PrivKeyFromBytes` uses), `SignTxOutput` with `KeyDb`/`ScriptDb` closures dispatching every signable script type (P2PK/P2PKH per suite, bare and P2SH-wrapped multisig with partial signing, all stake/treasury-tagged outputs), `mergeScripts`/`mergeMultiSig`, and `TSpendSignatureScript`. Differential across all 4 networks: raw signatures byte-for-byte (deterministic RFC6979/Ed25519), full `SignTxOutput` byte-for-byte over every shape incl. two-step multisig merge, with fully-signed results additionally executed through the engine. Not ported: dcrd's silent out-of-range secp256k1 key reduction (keys rejected instead) |
| `blockchain/stake` (tx primitives) | `dcroxide-stake` | vectors + diff | The transaction-level surface of `blockchain/stake/v5 v5.0.2`: classification and format checks for all stake types (`CheckSStx`/`CheckSSGenVotes`/`CheckSSRtx` plus the TADD/TSPEND/treasury-base checks and `DetermineTxType`, all 72 error kinds with dcrd's names), SStx commitment/change extraction (`TxSStxStakeOutputInfo`, commitment address/amount/fee-limit decoding), SSGen extraction (block voted on, vote bits, vote version, `'TV'` treasury-vote discriminator), the `Hash256PRNG` ticket lottery (IV derivation, draw/rollover semantics, `uniformRandom`, `findTicketIdxs`), the 64.32 fixed-point vote/revocation reward calculations including the PRNG-driven remainder distribution for auto-revocations, and `CreateRevocationFromTicket`. Parity: every `wire.MsgTx` vector from dcrd's `staketx_test.go` frozen with dcrd's own verdicts and replayed oracle-free (`tests/data/staketx_vectors.txt`, generated mechanically inside dcrd at the tag), the 100k-draw PRNG state vector and fixed reward tables from dcrd's tests, plus live differentials: full-surface analysis dumps over structured near-valid tickets/votes/revocations/treasury txs and their mutations, PRNG sequence + winner-selection byte-parity, reward amounts, and revocation construction byte-for-byte with error-kind parity. Also ported: the immutable ticket treap from `internal/tickettreap` — key-ordered by ticket hash and min-heap-ordered by the ticket height as the priority, with dcrd's exact rotation cascades (ties stay below on insert; the delete push-down picks the left child on equal priorities), structural sharing via reference counting in place of Go's garbage-collected pointers, iterative insert/delete rebuilds so degenerate sorted spines cannot exhaust the stack, `ForEachByHeight`'s heap-pruned iteration, `FetchWinnersAndExpired` (incl. its empty-index panic), and dcrd's node size accounting fixed at 64-bit pointer widths; pinned by dcrd's own tickettreap test battery ported in full (empty/sequential/reverse/unordered/duplicate/early-stop/snapshot with exact size accounting and the heap invariant; the unordered keys come from a deterministic generator rather than Go's sha256 counter, and the nil-value no-op `Put` is unrepresentable with by-value semantics). Also ported: the ticket database serialization formats from `internal/ticketdb` — the database info row (LE version with the upgrade bit, 32-bit truncated creation time), the best chain state (hash ‖ height ‖ live ‖ missed ‖ revoked ‖ per-block ‖ winner list, incl. the panic on more winners than the per-block count and zero-filled winner slots past the provided list), per-block undo data (37-byte entries with the packed missed/revoked/spent/expired flag byte), ticket hash lists, the individual ticket bucket row value, all nine error kinds with dcrd's names, and the bucket/key name constants; the `database.Tx`-coupled wrappers are now ported as the ticket database persistence layer over the dcroxide database (ffldb's exact bucket layout over redb): every `Db*` function (`DbCreate` with a caller-supplied creation date since dcrd stamps the wall clock, the database info, best chain state, per-height undo and maturing tickets rows, ticket bucket rows, and `DbLoadAllTickets`) plus the four entry points — `InitDatabaseState`, `LoadBestNode` (tip verification, treap reconstruction with count corruption checks, and the lottery final state recomputed from the tip header), `WriteConnectedBestNode` (the undo-driven bucket moves), and `WriteDisconnectedBestNode` (the above-tip row drops and the reverse-order undo application). Pinned by dcrd's own ticketdb serialization tests ported with their exact hex vectors, plus a persistence replay generated over a real ffldb inside dcrd's package: sixty connected blocks with misses, revocations, and expiries, ten disconnects restoring bookkeeping through the database rows, every bucket row compared byte for byte at five checkpoints, and `LoadBestNode` round trips matching on pool size, winners, missed tickets, undo data, and the recomputed final state. Also ported: the ticket pool state machine from `tickets.go` — the per-block stake `Node` over the live/missed/revoked treaps with `genesisNode`, `connectNode` (winner voting/missing, expiry sweeps via the heap-pruned iteration, revocations, ticket maturation, and the lottery draw of the next winners plus the 6-byte final state from the PRNG state hash), `disconnectNode` (reverse undo-data replay restoring the parent's winners and final state), the accessor surface (`SpentByBlock`/`MissedByBlock`/`ExpiredByBlock`/`ExpiringNextBlock` etc.), and `fetchWinners`; the database-coupled entry points (`InitDatabaseState`/`LoadBestNode`/`WriteConnectedBestNode`) arrive with engine persistence, making the `ErrMissingDatabaseTx` fallback unrepresentable. Pinned by 153 frozen state vectors from dcrd's own package: 120 connected blocks of purchases, votes with misses, revocations, and expiries over a small parameter set followed by 30 disconnects, comparing pool sizes, lottery final states, winner lists, and complete undo data at every step, plus the connect error kinds. Also ported: `checkTicketRedeemers` (votes must spend lottery winners, revocations must spend missed or expired tickets, and under the automatic revocations agenda every newly missed or expired ticket must be revoked; the nondeterministic error choice among multiple missing revocations in dcrd's map iteration is avoided in the vectors by crafting at most one), pinned together with the completed header stake commitments by 56 vectors incl. a real dcrd stake node built over a throwaway database attached to the shell chain. Also ported: `checkBlockContext` — the full contextual block assembly: `determineCheckTxFlags` (all five agenda flags), the coinbase/treasurybase first-transaction and unique-height rules, per-transaction context checks with the second-coinbase/second-treasurybase and misplaced-tree rules, the stake tallies (vote target block and vote-bits approval congruence, header vote/revocation/fresh-stake commitments, early-stake restrictions, treasury add and revocation bounds), cumulative quick sigop counting, and under full validation the agenda-driven `maxBlockSize`, merkle roots, transaction finality (past-median-time under the LN features agenda), the ticket redeemer checks against the parent stake node, and the treasury spend vote-interval/expiry-window rules; dcrd's recent-context-checks cache is a pure optimization and not reproduced — pinned by 36 crafted simnet blocks over 16 verdict kinds with the parent stake node rebuilt step-for-step through the ported ticket state machine (154 recorded connects whose pool size and final state match dcrd's exactly, doubling as a large-scale cross-validation of the state machine). Also ported: the utxo viewpoint from `utxoviewpoint.go` — entry addition with the unspendable-output and treasury-add-change exclusions and the ticket-submission minimal-outputs tails, transaction and block connect/disconnect producing and consuming the spend journal (incl. the in-flight zero-conf spend tracking in both directions, the reverse journal replay, and the disapproved-parent regular-transaction undo/redo on connect and disconnect), the input-fetching set construction with dcrd's exact in-flight index comparison, and the spent-output counting helpers; dcrd's utxo cache/database lookups are abstracted behind a caller-supplied resolver and the disapproved-parent journal is supplied directly — pinned by a full connect/disconnect round trip from dcrd's own package over a block that disapproves its parent while spending externals, an in-block zero-conf chain, a live ticket via a vote, plus a ticket purchase, treasurybase, and treasury spend, comparing every entry field, both spend journals, and the best hashes at each step. Also ported: `checkTransactionsAndConnect` — the per-tree connect loop composing the cumulative sigop checks, the fee-computing input validation with fraud proofs, and the view connection, then enforcing the vote-scaled fee penalty, the coinbase subsidy commitment and overpayment rules (`ErrBadCoinbaseAmountIn`/`ErrBadCoinbaseValue`), the treasurybase subsidy commitment, the empty-stake-tree rules, and the stakebase payout bound — plus `consensusScriptVerifyFlags` (agenda-driven script flags for CSV/SHA256/treasury) — pinned by 9 vectors over prepared simnet views with valid coinbase/vote/ticket/treasurybase trees and targeted mutations, incl. spend journal lengths. `ErrBadStakebaseValue` is unreachable with exact-payment votes and is exercised only through its bound; noted. Also ported: `checkConnectBlock` — the final connect battery: the treasury payout fork (`checkTreasuryBase` under the agenda, `coinbasePaysTreasuryAddress` before it), the stateless treasury spend checks (`tspendChecks`' vote-interval gate, expiry-window, and OP_RETURN value-in encoding rules; the duplicate-mine lookup, vote tallies, and expenditure bound need prior-block data and arrive with the chain engine), the disapproved-parent undo, both tree connects with the stake-tree fee carry-forward, sequence lock enforcement over both trees under the LN features agenda, the version 2 GCS filter built over the post-connect view and checked against the header stake root once header commitments are active (`CalcCommitmentRootV1` is the identity over the sole leaf), `checkBlockScripts` (sequential engine execution with the coinbase-input and version-2-auto-revocation skips; dcrd's parallel validator and signature cache are result-invariant optimizations and not reproduced), `blockOneCoinbasePaysTokens` against the block one ledger, and `calculateAddedSubsidy`; script execution is gated by a caller-supplied flag standing in for dcrd's bulk-import mode and assumed-valid ancestry — pinned by 36 vectors over a real 154-node simnet chain built inside dcrd's own package: a fully valid block connecting with a byte-identical filter hash and spend journal, the natural bad-commitment-root verdict, all four treasurybase kinds, coinbase and vote corruption propagating from the tree connects, an unmet relative sequence lock, the treasury spend window/value-in kinds, the pre-treasury organization payout kinds, the complete block one ledger battery, added-subsidy totals, and script validation over both trees with the auto-revocation skip both honored and refused |
| `blockchain/standalone` | `dcroxide-standalone` | vectors + diff | The full package at `blockchain/standalone/v2 v2.2.2`: merkle roots (copying and in-place variants, transaction trees over full hashes, the DCP0005 combined root), merkle inclusion proof generation/verification (incl. the 32-level proof-size cap), compact-bits conversions over arbitrary-precision signed integers with dcrd's exact semantics (negative encodings, beyond-256-bit exponents, and the floor-shift mantissa quirk for negatives), `CalcWork`, the three proof-of-work check functions with dcrd's error kinds, the DCP0011 ASERT difficulty algorithm (64.16 fixed-point + cubic approximation, ported step for step incl. Go's lossy `Int64` narrowing on extreme exponents), the complete subsidy cache (all three split regimes across V1/V2/V3 APIs, vote/treasury variants), treasury spend expiry/window math, and coinbase/treasury-base identification + `CheckTransactionSanity`. `wire.BlockHeader` gained `pow_hash_v2` (BLAKE3 per DCP0011). Parity: every dcrd test table ported (large tables extracted mechanically), dcrd's ASERT reference vectors replayed verbatim, all four total-supply schedule walks land on dcrd's exact totals, plus live differentials over random trees/txs/bits/ASERT params/subsidy surfaces/treasury windows and both pow hashes. Uses `num-bigint` for the big.Int-based API (documented in the crate docs); dcrd's RWMutex'd subsidy cache is `&mut self` here (result-invariant). The legacy EMA difficulty retarget lives in dcrd's `internal/blockchain` and arrives with the chain engine |
| `gcs` (+ `blockcf2`) | `dcroxide-gcs` | vectors + diff | The full `gcs/v4 v4.1.1` package: version 1 and 2 Golomb-coded set filters (bit-exact writer/reader, SipHash-2-4 via the `siphasher` crate matching dchest/siphash, the v1 modulo and v2 multiply-shift reductions, v2 empty-entry skipping and hash deduplication, big-endian-uint32 vs varint entry-count serialization), `Match`/`MatchAny`/`Hash`, `FromBytes` for both versions with dcrd's error kinds, `MakeHeaderForFilter`, `MaxFilterV2Size`, and the DCP0005 version 2 block committed filters (`blockcf2`: B=19/M=784931, merkle-root keying, the full entry-selection rules across regular and stake trees incl. ticket commitment→payment script conversion and the zero-value change exclusions). Parity: live differentials over random entry sets/keys for both versions (serialization, hashes, match verdicts) and over structured blocks with real ticket/vote/revocation/treasury transactions and randomized previous-script sets incl. missing-script error parity. Confirmed quirk (reproduced against dcrd live): dcrd's `MatchAny` panics with an index out of range when the search data contains empty entries alongside a filter entry above every hashed value; this port returns false for that case instead — the outcome for every non-panicking input is identical, and the panic is unreachable through the block filter paths |
| `database` (+ `ffldb`) | `dcroxide-database` | semantics | The `database/v3 v3.0.3` interface semantics over redb per accepted ADR-0004: all 22 error kinds with dcrd's names, transactions (managed `view`/`update` with dcrd's panic-on-commit/rollback semantics, unmanaged `begin`, single-writer/multi-reader, snapshot isolation, read-your-writes), buckets and cursors over ffldb's exact key layout (4-byte bucket IDs, `bidx` bucket index rows, `bidx-cbid` counter) so iteration order and nested-bucket behaviors match ffldb observably, flat `*.fdb` block files in dcrd's exact record byte format (LE network ‖ LE length ‖ block ‖ CRC-32C), block index rows as location ‖ header with headers always served from metadata, pending-block visibility within a transaction, and `reconcileDB`-style recovery (orphaned block-file bytes truncated on open; metadata-ahead-of-files is `ErrCorruption`). Parity instrument: the ported ffldb interface-test battery plus a crash-consistency rig (torn-write and missing-data tears, randomized tear soak) — a live Go oracle is the wrong tool for stateful storage. Documented divergences, all within the interface contract: no treap write cache, no LRU file-handle cache, snapshot cursors, and `Cursor::delete` on a read-only tx returns the contract's `ErrTxNotWritable` where ffldb silently discards it. ffldb quirks reproduced: empty-key `delete` silently succeeds; a bucket and a plain key may share a name. The `addblock` bootstrap format (LE net ‖ LE len ‖ block) is supported for storage-level bulk import (well-formedness checks, duplicate skipping, batched transactions; consensus validation layers on with the chain engine) and byte-identical export. Since ported: the compressed UTXO encodings (with the blockchain serialization piece).  Not yet ported: the database-backed UTXO backend (`utxobackend.go`, chain-engine wiring) |
| `internal/blockchain` | `dcroxide-blockchain` | vectors | The UTXO serialization layer from `compress.go`, `utxoentry.go`, `utxoio.go`, and `chainio.go`'s minimal-outputs size reader: VLQs (MSB base-128 with the offset subtraction), the domain-specific script compression (six special forms incl. uncompressed-pubkey point recovery via dcroxide-dcrec; unrecognized scripts behind VLQ(len+64)), the base-10 exponent amount compression, compressed txout encode/decode with dcrd's error text, txout/entry flag bytes (raw stake-type bits survive round trips like dcrd's unchecked cast), `UtxoEntry` with its state/flag accessors, outpoint keys (prefix [3,3] ‖ hash ‖ VLQ tree ‖ VLQ index), entry serialization incl. the ticket-submission minimal-outputs tail, and the UTXO set state row. dcrd keeps these in an internal package the oracle cannot import, so parity is pinned by dcrd's own test tables extracted mechanically from the Go test sources (57 vectors) plus randomized round-trip properties. Quirk noted: dcrd's `putCompressedScript` opens with a dead always-panic `len(target) == 0` branch; unreachable via any caller and not reproduced. Also ported: the legacy difficulty algorithms from `difficulty.go` — the exponentially-weighted BLAKE-256 work retarget (64.32 fixed point, genesis-parking window walks, testnet reduce-min-difficulty and max-difficulty rules), the BLAKE3 (DCP0011) difficulty from an anchor node, `mergeDifficulty`, `estimateSupply`, and both stake difficulty algorithms (original and DCP0001), with the chain walk abstracted behind a height-indexed `ChainView`. Parity: 35 synthetic-chain scenarios generated inside dcrd's own package with dcrd's outputs frozen and replayed (three networks, retarget-boundary lengths), plus pure-function samples. Also ported: the stake version machinery from `stakeversion.go` plus `calcWantHeight` and the 11-block past-median-time (even-count quirk preserved) — interval-final node location, header stake version majorities, prior stake version, voter version interval walks, block version majorities, and `calcStakeVersion` including its no-return nil-ancestor fall-through; pinned by 18 synthetic simnet chains generated inside dcrd's package plus 60 `calcWantHeight` samples, all replaying exactly (dcrd's per-hash memoization caches are result-invariant and not reproduced). Also ported: the threshold state machine from `thresholdstate.go` (`nextThresholdState` with the full Defined→Started→LockedIn→Active/Failed transitions, vote tallying with choice-index masking, quorum and activation thresholds, deployment version lookups, and forced-choice resolution for test networks), pinned by 195 state-and-choice verdicts over 15 synthetic simnet chains covering every deployment, all replaying exactly; the per-deployment hash-keyed cache is recomputation-equivalent on a single-branch view and not reproduced. Also ported: the agenda activation helpers and algorithm selectors (`is*AgendaActive` for all ten vote IDs incl. the treasury genesis special case and the forced-choice fast path, the BLAKE3 work-difficulty anchor search, and the `calcNextRequiredDifficulty`/`calcNextRequiredStakeDifficulty` selectors), pinned by 156 verdicts over 12 synthetic simnet chains from dcrd's own package. Also ported: the chain persistence serialization formats from `chainio.go` — ticket minimal outputs, block index keys/entries (header ‖ status ‖ VLQ vote tuples), the spend journal (reverse-serialized compressed stxos with the vote-stakebase skip and ticket minimal-outputs tails), header commitments, and the best chain state (LE fields ‖ stripped big-endian work sum, incl. the all-zero-keeps-32-bytes quirk and SetByteSlice truncation on load) — pinned by 150 vectors from dcrd's own package with Go-side round trips proven before freezing. Also ported: all 158 consensus rule error kinds (machine-generated) and the context-free validation layer from `validate.go` — `CheckTransaction` (standalone sanity + the agenda-driven context checks across coinbase/vote/revocation/treasury shapes, script version and stake-opcode rules), `CheckProofOfStake`, and the dual-hash `checkProofOfWorkSanity` — pinned by 520 verdicts over structured and mutated transactions, blocks, and headers from dcrd's own package. Also ported: the block sanity layer — `checkBlockHeaderSanity` (PoW sanity, the +2h future-time limit against an adjusted-time parameter standing in for dcrd's `MedianTimeSource`, the pre-stake-validation-height commitments incl. the 0x0001 early vote bits and zero final state, and the vote/fresh-stake count limits; dcrd's sub-second timestamp precision check is unreachable here since wire timestamps are whole seconds by type) and `checkBlockSanity` (proof of stake, non-empty regular tree, `MaxBlockPayload` and header size commitment, per-tree standalone transaction sanity, the fresh-stake header commitment tally, and duplicate transaction detection) — pinned by 400 verdicts over boundary-biased headers and assembled blocks from dcrd's own package. Also ported: the sequence lock (relative lock time) calculation from `sequencelock.go` — `calcSequenceLock` (DCP0003: per-input disable/seconds/mask bit handling, mempool-height inputs viewed from the next block, block-based locks from input heights, time-based locks from the past median time of the block before the input's, and the coinbase/stakebase/version<2 exemptions incl. the internally derived treasury agenda state) and `LockTimeToSequence` — with dcrd's `UtxoViewpoint` lookup abstracted to a confirmation-height closure until the chain engine lands; pinned by 264 vectors over synthetic simnet chains (treasury forced-active there) and mainnet chains (inactive branch) from dcrd's own package. Also ported: the positional validation layer — `checkDifficultyPositional` (the forced-BLAKE3, EMA, and candidate-anchor-walk paths, incl. dcrd's signed-to-unsigned header version cast; dcrd's two cached BLAKE3 anchors are not reproduced: the candidate cache is a pure optimization and the confirmed-anchor cache is an engine fast path), `checkBlockHeaderPositional` (past-median-time, the testnet3 minimum-time rule and the height-962928 max-diff checkpoint, height commitments, and old-version-by-majority rejection with the DCP0005 violation exemptions and all six pinned block hashes; the fork rejection checkpoint check landed with the headers-first processing layer once the block index arrived), `checkBlockDataPositional`/`checkBlockPositional` (expired transactions), and the pure helpers (`voteBitsApproveParent`, `IsExpiredTx`, `SequenceLockActive`, `IsFinalizedTransaction`) — pinned by 276 vectors over six scenario chains spanning all four networks, incl. a base-offset testnet3 chain around the max-diff activation height and regnet headers ground to solve BLAKE3 so the candidate anchor walk is exercised both succeeding and falling back. Found and fixed in the process: the difficulty code's testnet3 detection used the old testnet2 network magic (`0x48e7a065` instead of wire's `0xb194aa75`); the frozen difficulty vectors were unaffected (regenerated identical) but the max-difficulty clamp would never have applied on real testnet3 data. Also ported: the contextual header validation layer — `checkProofOfWorkContext` (hash algorithm selected by the DCP0011 agenda state), `checkBlockHeaderContext` (agenda-driven work and stake difficulty, the version-3 stake version majority enforcement, and — now that the ticket state machine is ported — the pool size and ticket lottery final state commitments against the caller-supplied parent stake node state), `checkMerkleRoots` (combined root under DCP0005 header commitments, dual roots otherwise), the `is{Blake3Pow,HeaderCommitments}AgendaActive` wrappers, and the coinbase/treasurybase unique-height checks (`checkCoinbaseUniqueHeight`/`checkTreasurybaseUniqueHeight` with the consensus-specialized OP_RETURN null-data parsing, incl. the 256-byte cap and 4-byte little-endian height) — pinned by 175 vectors over simnet (both agendas forced active), mainnet, and regnet chains with headers ground for the agenda-correct hash algorithm and crafted null-data scripts. Also ported: the stake transaction input validation layer — the stake-tagged P2PKH/P2SH script extractors, `isAllowedTicketInputScriptForm`/`isStakeSubmission`, the ticket commitment field extractors (hash/amount with the P2SH flag bit/fee limits), `checkTicketPurchaseInputs` (dcrd's `UtxoViewpoint` lookup abstracted to a `UtxoEntry`-returning closure), `calcTicketReturnAmounts` (64.32 fixed point over arbitrary precision with Go's lossy `Int64` narrowing, incl. the auto-revocation remainder distribution via Hash256PRNG seeded with the previous header), `checkTicketSubmissionInput`, `checkTicketRedeemerCommitments` (payee type/hash/value against the ticket commitments with the log2 fee limit semantics incl. the ≥63 full-fee rule and the treasury-vote output exclusion honoring Go's nil-slice `CheckSSGenVotes` semantics), and `checkVoteInputs`/`checkRevocationInputs` (stakebase subsidy commitment via the V3 subsidy cache with a `ChainSubsidyParams` adapter over the chain parameters, submission input/index/maturity rules with the +1/+2/auto-revocation variants) — pinned by 149 vectors over crafted tickets, votes, and revocations against a fabricated utxo view from dcrd's own package. Also ported: `CheckTransactionInputs` — the input validation orchestrator returning the transaction fee: coinbase/treasurybase early-outs, delegation to the ticket/vote/revocation input checks, treasury spend Pi-key validation (`verifyTSpendSignature` over the empty-script `SigHashAll` sighash with EC-Schnorr-DCRv0), the stakebase-subsidy and tspend value-in accumulation exceptions, zero-value spend bans, the fraud proof triple, coinbase/expiry/OP_TGEN/OP_SSGEN/OP_SSRTX/stake-change maturities (with the treasury-dependent stake output maturity), the OP_SSTX spend restriction, the unspendable treasury add rule, and the amount range/overflow/fee accounting — pinned by 88 vectors on simnet incl. treasury spends actually signed with the published simnet Pi test key, all replaying exactly. Also ported: the signature operation and stake amount accounting — `CountSigOps` (imprecise per-script counting with the stakebase/treasurybase skips), `CountP2SHSigOps` (precise redeem-script counting over the utxo lookup with the overflow guard), `checkNumSigOps` (cumulative enforcement against the 5,000 per-block limit), `checkStakeBaseAmounts`/`getStakeBaseAmounts` (vote subsidy limits aligned to the height voted on), `getStakeTreeFees` (stake-tree fee summation with the vote-subsidy/tspend/treasurybase adjustments), and `checkDupTxs` (a no-op at this dcrd version per its disabled `checkForDuplicateHashes` constant, ported with the dormant logic) — pinned by 195 vectors over sigop-bearing P2SH scripts and vote sets from dcrd's own package. Also ported (beginning the Phase 8 chain engine proper): the in-memory block index and chain view from `blockindex.go`/`chainview.go` — block nodes with the deterministic skip-list ancestor traversal, accumulated work sums (via the pinned `CalcWork`), the stable status bit flags, header reconstruction, lottery IVs, past median times, and the `betterCandidate` ordering (work, then data availability, then received order, then the little-endian-uint256 hash tiebreak); the block index with chain tip tracking, best header/invalid selection, best chain candidates, unlinked-children data linking (`AcceptBlockData`), and `MarkBlockFailedValidation`'s descendant invalidation walk over the (cached) chain tips; and the height-indexed chain view with efficient tip switching, fork finding, and block locator construction (dense-then-doubling with `fastLog2Floor` sizing). Go's parent-pointer graph is an arena (`NodeStore` with index handles) shared by the index and views; dcrd's short-key/collision map pair (a memory optimization), mutex wrappers, database flush machinery, and wall-clock cached-tips prune timer are documented as not reproduced. Parity: dcrd's own blockindex_test.go and chainview_test.go batteries ported in full (header round trips, the median time table, chain tips, the 250,000-node skip list with 2,500 random probes, the ancestor relationship table, the complete betterCandidate table, three-branch view containment/next/fork/locator layouts, tip switching, and empty views), plus a supplementary invalidation-and-linking test for the paths dcrd covers via integration tests. Also ported (the chain engine scaffolding proper): the headers-first processing layer from `process.go` — a `Chain` state over the arena-backed block index carrying the assumed-valid and old-fork-rejection configuration (`expectedBlocksInTwoWeeks` derived from the target block time, fork rejection disabled when configured off or the network has no hard-coded assumed valid hash), `checkKnownInvalidBlock`, `maybeAcceptBlockHeader`/`ProcessBlockHeader` (known-status short circuit, optional header sanity, orphan and invalid-ancestor rejection, the positional checks, index insertion; dcrd's block index database flush arrives with persistence wiring), automatic fork rejection checkpoint discovery (`maybeSetForkRejectionCheckpoint`), assumed-valid node tracking (`maybeUpdateAssumeValid`), and `isAssumeValidAncestor` with its two-weeks-behind-best-header clamp — plus the `NodeBranchView` adapter that walks a block tree branch through the deterministic skip list to serve the height-indexed chain views every previously ported validation function consumes, and the completed `ErrForkTooOld` check inside `checkBlockHeaderPositional` (the fork rejection facts dcrd reads from its index mid-check are supplied by the processing layer; a `None` checkpoint skips the check, keeping the earlier frozen vectors byte-compatible). Pinned by 48 state-machine vectors from dcrd's own package: a 30-header simnet chain with ground proofs of work fed through the real `maybeAcceptBlockHeader` over a real block index, covering acceptance, duplicates, orphans, sanity failures honored and skipped, positional failures, side chains above and below the discovered checkpoint, a mid-chain invalidation with the known-invalid short circuits, and the clamped assumed-valid ancestry checks, comparing verdicts, node status bytes, the best header, and the tracked checkpoint and assumed-valid nodes at every step. Also ported: the stake node attachment layer from `stakenode.go` — `maybeFetchNewTickets` (the maturing tickets loaded by walking back the ticket maturity distance, with the `None`-versus-empty distinction preserved), `maybeFetchTicketInfo` (vote and revocation info populated from the block data via the already ported `FindSpentTicketsInBlock`; dcrd tracks population through slice nil-ness, an equivalent flag here since the fields are populated together and repopulation is idempotent), and `fetchStakeNode` with all three of its paths: the cache hit, the parent connect, and the full regeneration walk that disconnects from the current best chain tip back to the fork point — restoring each pruned node's own bookkeeping from the per-height ticket database undo and tickets rows exactly as dcrd loads them inside the disconnect — and then replays any side chain blocks up to the requested node; the `Chain` state grew the best chain view, the block storage, and the per-height ticket rows (in-memory stand-ins for dcrd's database, recent-block cache, and `WriteConnectedBestNode` rows until the persistence wiring lands), and block nodes now carry the optional stake node and maturing tickets like dcrd's. Pinned by 215 vectors from dcrd's own package: a 154-block simnet chain of real skeleton blocks (ticket purchases maturing on schedule, votes with random misses, and revocations) advanced through the real `fetchStakeNode` with ticket database rows written at every height, a side chain forked below the tip, then 55 prunes followed by fetches that force the tip-to-fork disconnect walk over the undo rows, the side chain replay, the fork-point path, and the cache hit — comparing pool sizes, lottery final states, winner lists, and missed tickets. Also ported: the reorganization engine from `chain.go` — `BestState`/`newBestState` (incl. the genesis initial snapshot from `createChainState`), `connectBlock` (best state accumulation of transactions and added subsidy, the spend journal record, ticket database rows, filter, and header commitment leaves, the view commit, and the tip move; the treasury balance and treasury spend rows arrive with the treasury database, and notifications, cache flush tuning, the block index flush, and the stake node memory prune optimization are not reproduced), `disconnectBlock` (the parent snapshot with the disconnected block's own stake difficulty commitment, the ticket row drops above the new tip per `WriteDisconnectedBestNode`, and the journal record removal), `loadOrCreateFilter`, `reorganizeChainInternal` (the tip-to-fork detach loop over the spend journal and the attach loop with the validated fast path and the full `checkBlockContext` + `checkConnectBlock` path marking failed validation), `reorganizeChain` with the failed-reorg fallback to the best valid candidate, and the is-current latch (`isOldTimestamp`/`maybeUpdateIsCurrent`/`isCurrent` against a caller-supplied adjusted time).  Three pieces of dcrd's utxo cache semantics proved observable and are reproduced exactly over the in-memory backend+cache stand-ins: the spend journal serialization is round tripped because dcrd reconstructs spent entry amounts, heights, and indexes from the spending inputs' fraud proofs on load; fresh (never flushed) cache entries are removed outright on spend with an explicit spent marker while flushed entries become tombstones that disconnects restore in place with original fields; and the forced cache flush on every disconnect drops the remaining tombstones so blocks detached later in the same reorganization resurrect their outputs from the journal instead.  Pinned by 771 vectors generated against a complete real `BlockChain` (`New()` over real block and utxo databases): two competing branches of pre-validated skeleton blocks reorganized forward, across, back, and extended, comparing the best state snapshot and all 732 presence-and-field checks over the full utxo universe after every reorganization.  Also ported: the full block intake path completing `process.go` — `maybeAcceptBlockData` (prunable ticket info population, the positional data checks with failed-validation marking, block storage, and the index data acceptance producing the newly fully-linked descendants; the stake node pruner is a memory optimization and not reproduced), `maybeAcceptBlocks` (contextual checks over each linked block with failed-validation marking and first-error semantics; the recent block/context caches and notifications are not reproduced), and `ProcessBlock` itself (duplicate and known-invalid rejection, full block sanity, header acceptance reusing the headers-first path, the bulk-import/assumed-valid fast path that pre-marks validation, data acceptance, tentative acceptance of all linked blocks, reorganization to the best chain candidate regardless of acceptance failures exactly like dcrd, and the fork length result).  Pinned by 261 vectors generated against a complete real `BlockChain` driving the real `ProcessBlock`/`ProcessBlockHeader` over ground proof-of-work simnet blocks (the sanity checks accept either proof of work hash while the contextual checks demand the agenda-selected BLAKE3 hash, so the generator grinds for both): sequential intake, a duplicate, an orphan, a headers-first delivery with out-of-order data linking, a mid-processing reorganization to a longer branch, sanity and positional rejections, a full-validation connect failure with the known-invalid (reported as a duplicate per dcrd's check order) and invalid-ancestor short circuits, and a losing-branch extension — comparing verdicts, fork lengths, node status bytes, best state snapshots, and the final utxo universe.  Also ported: the manual chain manipulation surface — `InvalidateBlock` (the genesis and unknown block rejections, the already-failed no-op, the mark-only path for side chain blocks, and for best chain blocks the rollback to the parent, the descendant invalidation, the current-latch reset, the best chain candidate rescan over all chain tips walking each back to its first valid validatable ancestor, and the reorganization to the best remaining candidate), `ReconsiderBlock` (clearing invalidity up the ancestor chain while tracking the earliest failure, clearing the invalid ancestor flag across descendants that are not otherwise invalid, restoring candidates and unlinked children entries, recomputing the best invalid block and best header over all tips, and reorganizing), and `forceHeadReorganization`/`ForceHeadReorganization` (the same-block, wrong-chain, missing-child, known-invalid, and no-data rejections before reorganizing to a tip sibling).  Pinned by 37 operation vectors (plus the intake and 201 utxo universe rows) against a complete real `BlockChain`: sibling force-reorgs with every error kind, side chain marking round trips, a best chain invalidation whose fallback reorganization itself fails because invalidation clears the validated status and the skeleton blocks cannot pass revalidation (the chain settles on the deepest valid tip), the no-op repeat, reconsiderations that fail revalidation and stay put, and a deep invalidation taking every branch with it — comparing verdicts, tips, the status byte of every block after every operation, and the final utxo universe.  Also ported: the chain state persistence — `createChainState` (every bucket, the versioned database info rows, the genesis block index row, best state, stake database initialization, the stored genesis block and its empty filter, and the deployment version row) and the `initChainState` load path (the version downgrade rejections, height-ordered block index reconstruction incl. the new-consensus-rules failed-block unmarking, the best chain and candidate restoration, the stake node load, and the best state snapshot rebuild — reproducing dcrd's own restart quirk where `NumTxns` is recomputed from the regular transaction tree only), `flushBlockIndex` over the tracked modified nodes with pruned ticket info repopulation, and write-through persistence in the connect, disconnect, and cache flush paths (best state, spend journal, ticket database rows, filters, and commitment leaves; dcrd houses the UTXO set in a separate database with its own backend while dcroxide colocates it in a dedicated bucket of the one redb database using the same pinned row formats — a fresh-sync schema decision — and the chain data is currently held in memory with write-through rather than lazily read back, an optimization that arrives later).  dcrd's `upgradeDB` migration paths are not applicable to dcroxide's fresh-sync databases and are intentionally not ported.  Pinned by the reorganization ground truth replayed over a database-backed chain with a flush, restart, and reload at the end: the frozen final best state and utxo universe hold against the reopened chain along with the block index statuses, spend journals, filters, tip, and stake state (restarting mid-scenario is deliberately not compared against the frozen fields because utxo cache tombstones do not survive restarts, so continued reorganizations legitimately resurrect entries from the journal fraud proofs — a timing divergence dcrd itself exhibits).  Also ported: `CheckConnectBlockTemplate` (the tip-or-parent template parent rule, sanity without proof of work, the positional and contextual checks, and the full connect checks — over the as-is chain state when extending the tip and over the tip block's spend journal undo when building on its parent; templates never enter the block index so no scaffolding nodes are created), `checkTicketExhaustion`/`CheckTicketExhaustion` (the maturity-window live pool projection with the pre-stake-validation voting block adjustment), and the chain query surface (`MainChainHasBlock`, `BlockHeightByHash`, `BlockHashByHeight`, `HeaderByHash`/`Height`, `BlockByHash`/`Height`, `MedianTimeByHash`, `ChainWork`, `TipGeneration`, and `HeightRange`).  Pinned by 172 vectors against a complete real `BlockChain`: seven template verdicts (a valid template on the tip before and after the commitment root fixup, a valid template on the tip's parent exercising the journal undo path, and the invalid-parent, duplicate-transaction, height-commitment, and coinbase-subsidy rejections), eight ticket exhaustion verdicts over a header-only chain approaching stake validation height (thresholds, rescue by sufficient purchases, and the unknown block), and the query surface outputs.  Also ported: the treasury account and spend tracking from `treasury.go` — the treasury state rows (the balance as of each block plus its yet-to-mature values with dcrd's exact VLQ serialization incl. the negative-debit storage convention and sign sanity checks), the treasury-spend-to-blocks rows, `calculateTreasuryBalance` (the coinbase-maturity window), the per-block record writes wired into the block connect path, `checkTSpendExists` with fork awareness, `tSpendCountVotes`/`checkTSpendHasVotes` (the window walk over vote-carrying blocks with the quorum and remaining-votes short-circuit math), `sumPastTreasuryChanges`, all three expenditure policies (DCP0006 window averages with the bootstrap fallback, DCP0007 income-based, and DCP0013 balance-percentage with the spend limit floor) with the agenda-driven selection, `checkTSpendsExpenditure`, and the complete `tspendChecks` — replacing the stateless subset with the full window, value-in, duplicate-mine, vote tally, and expenditure battery.  Pinned by 264 vectors against a complete real `BlockChain`: a 240-block simnet chain with treasurybases, treasury adds, treasury-vote-carrying votes across a full voting window, and a spend mined mid-window — treasury balances at five depths, exists checks across the mine boundary, vote tallies mid-window and at the window end, maximum expenditures at two vote interval boundaries, all five `tspendChecks` verdict kinds (ok, exists, expenditure, votes, and the non-interval no-op), and the raw treasury rows byte for byte.  The DCP0006 and DCP0007 expenditure paths are exercised structurally but not pinned against dcrd on simnet since the DCP0013 agenda is force-activated there; they await the full-chain cross-validation work.  Cross-validated end to end: dcrd’s own full block test battery (`fullblocktests.Generate`) replayed through the real `ProcessBlock` with full validation on — 573 instances over a scripted regression network chain of fully signed blocks with real ticket purchases, votes, revocations, P2SH redemptions, and reorganizations: 204 accepted blocks matching dcrd’s main-chain and orphan verdicts, 350 rejections matching dcrd’s exact rule error kind across 61 distinct kinds (incl. `ErrScriptValidation` from real signature failures), the orphan-or-reject instances, 14 expected-tip checkpoints, and the non-canonical block encoding rejected at the wire layer.  The dump records its generation wall clock so the too-far-in-the-future rejection replays, and the nanosecond-precision timestamp rejection is a documented skip: wire timestamps are whole seconds by type, so the instance only exists for in-memory blocks and dcrd itself cannot hit it from serialized bytes either.  Also ported (completing the `internal/blockchain` surface): the RPC and network-sync query layer — `stateLastChanged`/`StateLastChangedHeight` (the interval-by-interval walk back from the current confirmation interval), the public `NextThresholdState`, `GetVoteCounts`/`CountVoteVersion` (invalid vote choices tallied as abstains, wrong-version votes ignored), `GetStakeVersions` (with dcrd’s count clamp and error shapes), `GetVoteInfo`, the block locator surface (`locateInventory`/`LocateBlocks`/`LocateHeaders` with the wire max-headers cap, and `BlockLocatorFromHash` falling back to the best tip), and the RPC stake difficulty estimators (`estimateNextStakeDifficultyV1` with its fake-chain overlay distributing the estimated ticket purchases across the remainder of the interval, `estimateNextStakeDifficultyV2` over the DCP0001 pool projections, and the agenda-driven `EstimateNextStakeDifficulty` selector).  Pinned by 1,464 vectors over three deterministic fake chains generated inside dcrd’s own package: a mainnet block index with a side chain replaying dcrd’s own locator test layout, a 1,200-block regnet chain that runs the DCP0001 agenda through Started, LockedIn, and Active with real votes (wrong-version and invalid-choice votes included) exercising both estimator algorithms on either side of the activation and the real state-change walks, and the simnet forced-choice fast paths — the fake chains replay hash-identical.  Also ported: the committed filter serving from `headercmt.go` — `FilterByBlockHash` (the version 2 GCS filter for a block with its header commitment inclusion proof, gated on the block being known with data available, surfacing the no-filter error kind otherwise) and `LocateCFiltersV2` (the batched cfilters response over an inclusive block range, with the unknown-block, not-an-ancestor, and over-the-maximum-batch error gates, the parent walk collecting the range hashes, and the per-filter inclusion proofs), both serving from the in-memory filter and header-commitment caches the engine keeps in step with the database; `Chain::new` now caches the genesis block's committed filter to match `createChainState` and the open path, so the genesis filter is served like any other block's.  Pinned by a real regnet chain built from dcrd's own fullblocktests generator (fifteen blocks) with dcrd's `FilterByBlockHash` and `LocateCFiltersV2` exercised directly: the block bytes are frozen so the engine here rebuilds the same chain and serves byte-identical filters, proof indices, and proof hashes across mid-chain, tip, and genesis blocks, a range and single-block batch, and the unknown-block/not-ancestor/no-filter error kinds; the server `OnGetCFilterV2`/`OnGetCFiltersV2` handlers wrap these (pushing the response on success and ignoring the error otherwise, confirmed in the dump by driving dcrd's real handlers over a piped serverPeer).  Also ported: the live-ticket queries from `stakeext.go` — `CheckLiveTicket`, `CheckLiveTickets`, and `LiveTickets` delegating to the best node's stake state, and the `BestSnapshot` accessor.  Pinned by a real regnet chain built past the stake validation height from the fullblocktests generator (one hundred fifty blocks): the full ordered live ticket set (two hundred eighty-five tickets) matches dcrd's `LiveTickets` byte for byte, the per-ticket and batched membership checks agree for live tickets, an unknown hash, and a block hash, and the best snapshot hash and height match.  Also ported: `LotteryDataForBlock` — the next tickets eligible for voting, the ticket pool size, and the lottery PRNG final state for a block's stake node, empty below stake enabled height and returning the unknown-block error otherwise.  Pinned over the same real past-stake-validation regnet chain: the tip's winning tickets, pool size, and final state match dcrd byte for byte, as do the empty-winner rows for blocks between stake enabled and stake validation heights (with their growing pool sizes), the blank data below stake enabled height and for the genesis block, and the unknown-block error.  Also ported: `TicketPoolValue` — the sum of the stake submission output amount of every live ticket, resolved through the utxo set; pinned over the same real chain against dcrd's exact total (five million seven hundred thousand atoms across two hundred eighty-five tickets).  The only `internal/blockchain` machinery not reproduced is documented caches and plumbing: the BLAKE3 anchor caches, the memoization maps, the database upgrade paths (fresh-sync), and the mutex/notification scaffolding |
| `internal/blockchain/indexers` | `dcroxide-indexers` | vectors | Complete: the shared indexer machinery (the `idxtips` bucket with its 36-byte hash+height tips, the `v`-prefixed version keys and `d`-prefixed drop markers, index creation/upgrade, the interrupted-drop resume on init, the incremental flat drop in 2,000,000-entry batches, and main-chain recovery by repeated tip disconnection), the index update subscriber with prerequisite/dependent subscription chains, the `updateIndex` expected-height state machine (lower heights relayed to the dependent with the relay error deliberately discarded exactly as dcrd does, higher heights failing with `ErrMissingNotification`), and `CatchUp` from the lowest index tip; the version 2 transaction index (three buckets: 48-byte entries under `txbyhashidx` keyed by tx hash with block id/offset/length/tree index, plus the `idbyhashidx`/`hashbyididx` block-ID compaction pair, the forward-scan-plus-binary-search highest-ID recovery on init, and the `TxLoc` offsets derived from the serialized block exactly as `DeserializeTxLoc`); the version 2 exists address index (21-byte type+hash160 keys with empty values under `existsaddridx`, the four address-type foldings incl. pay-to-pubkey collapsing onto its pubkey-hash twin, multisig redeem script extraction from signature scripts, ticket commitment addresses for both P2PKH and the amount-MSB P2SH flavor, the never-remove disconnect semantics, and the memory-only unconfirmed overlay flushed into the bucket on connect); and the legacy `txbyaddridx`/`cfindexparentbucket` drop helpers.  dcrd's buffered-channel goroutine delivery and 500ms sync ticker are daemon-phase concurrency; this port delivers synchronously with identical state transitions, and the sync-waiter signalling is covered natively.  Pinned by a 4-scenario scripted session generated inside dcrd's own package over its `testChain` and chaingen blocks, replayed against a real redb-backed database: creation, catch-up, connects, disconnects, a reorg with recovery on re-init, drop and re-creation, entry queries, an address-soup block covering every key type, the unconfirmed overlay, independent and dependent subscriptions incl. the lagging-dependent relay, and the error kinds — comparing the index tips and the full byte-for-byte contents of every index bucket after each operation |
| `internal/mempool` | `dcroxide-mempool` | vectors | Started: the mempool error identities (all 22 `ErrorKind`s with dcrd’s exact names incl. the historical "ErrorAlreadyVoted" spelling, and the `RuleError` that wraps either a mempool kind or an underlying chain rule error with dcrd’s `wrapTxRuleError` kind-retention semantics) and the relay policy layer from `policy.go`: `calcMinRequiredTxRelayFee` (with the Go overflow wrap clamped to the max amount), `isDust` (the 6030-atom p2pkh boundary at the default relay fee, unspendable outputs, and wrapped arithmetic on extreme values), `checkPkScriptStandard` (version gate plus the 1-to-3-key multisig limits), `checkTransactionStandard` (serialize type, finality, size, signature script size and push-only rules with the treasury spend input skip, output standardness with the dust and null-data-count exemptions by transaction type), and `checkInputsStandard` (referenced script forms and the 15-sigop P2SH limit, with the vote and treasury-spend first-input skips; the utxo view lookup is a closure like the other engine-coupled ports).  Pinned by 69 verdicts from dcrd’s own package over its policy test tables plus boundary, overflow, and skip-semantics cases, incl. input checks against a real `UtxoViewpoint`.  Also ported: the `TxPool` itself — the main, orphan, and stage pools with their outpoint indexes, vote and treasury spend tracking, the complete acceptance gauntlet (`maybeAcceptTransaction`: duplicate and known-transaction checks, the chain sanity and input validation delegated to the ported blockchain layers, expiry, the stake-type height gates, standardness, the ticket stake difficulty gate, pool/vote/revocation double spend checks, old vote policy, the already-exists check over the fetched view, orphan detection with fraud proof refresh, sequence locks, sigop limits, both fee policy directions, script verification via the newly ported single-transaction `ValidateTransactionScripts`, and the treasury spend expiry/limit/Pi-key policy), orphan processing with cascading acceptance and double spend eviction, ticket staging with unstaging through `MaybeAcceptDependents`, batch acceptance over the transient pool (`MaybeAcceptTransactions`), the stake and expiry pruners, and the removal/query surface.  The chain callbacks live behind a `PoolChain` trait standing in for dcrd’s config closures; the mining view bookkeeping, exists address index, fee estimator hooks, and notification callbacks are integration plumbing deferred to their phases, and dcrd’s time-driven orphan expiration and randomized orphan eviction are ported but deliberately untriggered by the vectors since dcrd itself documents the eviction order as arbitrary.  Pinned by a 96-row scripted session against dcrd’s own pool test harness (`newPoolHarness` with real signed transactions over a mirrored fake chain): chained acceptance, orphan cascades and policy, duplicates, double spends, already-exists, fee policy both directions, block- and time-based sequence locks, expiry with pruning, ticket staging and unstaging on parent confirmation, stake difficulty gates with pruning, batch acceptance, and double spend removal — comparing every verdict, accepted list, and the full pool/orphan/stage state after every operation.  Notable dump landmine: the coinbase height lives in the signature script, which is witness data excluded from the transaction hash, so distinct funding coinbases must vary their prefixes or they alias the same outpoints.  Also pinned: the stake acceptance scenarios over a second 137-row scripted session from dcrd’s own harness — the vote orphan flow (vote rejected as an orphan, the ticket arriving as an orphan itself, the funding transaction cascading the ticket into the stage pool, and the mined ticket unlocking the vote), duplicate votes on the same block, the five-vote double spend limit across candidate blocks with recovery after removal, old vote policy, the vote and revocation stake validation height gates, revocation acceptance alongside votes with the one-revocation-per-ticket rule, disapproval tallies over no-votes, the votes-map retention quirk after pruning (dcrd deliberately keeps vote metadata for blocks after the votes leave the pool), and the treasury battery with a replaced Pi key set: the seven-concurrent-tspend limit with recovery, mined-on-ancestor rejection, the expiry policy in all three directions, the chain-level `ErrInvalidPiSignature`/`ErrUnknownPiKey` rejections, acceptance up to the final voting window block, treasury adds under the fee policy, and standalone treasurybase rejection.  The mining view bookkeeping remains with the mining phase |
| `internal/fees` | `dcroxide-fees` | vectors | The smart fee estimator behind `estimatesmartfee`: exponentially spaced fee rate buckets (incl. the extra-bucket insertion and the +inf catch-all), decaying confirmation tracking with dcrd’s exact floating point accounting, mempool tracking with the deliberate integer-division rate downsampling and the tspend/below-minimum exclusions, mined transaction processing across confirmation ranges (incl. height gaps and stale block rejection), the median fee estimation with all four error kinds, and the database bucket row codec (big-endian float bits).  The leveldb persistence plumbing arrives with the daemon wiring; the row format is pinned now.  Pinned by 425 rows from dcrd’s own package over two estimator configurations: every estimate and raw median compared, and four complete bucket state snapshots (confirmed and mempool) compared bit for bit through the row codec — the floating point decay math replays exactly |
| `internal/mining` | `dcroxide-mining` | vectors | Complete: the mining view layer — the transaction descriptor types (`TxDesc`, `TxAncestorStats`, `VoteDesc`, `UnminedHeight`), the dependency graph (`txDescGraph`) with its topological walks, the `TxMiningView` with ancestor statistics tracking bounded by the 25-entry limit (incl. the retrack-on-removal and descendant-pruning edge behaviors), cloning with source isolation, rejection cascades, the transaction priority queue with a faithful port of Go's `container/heap` sift operations so pop order matches dcrd exactly incl. among equal elements, the stake-priority ordering (votes over automatic revocations over tickets), and `CalcPriority` with the input-age math.  The redeemer/locator closures dcrd stores in the graph are passed per call for ownership; behavior is identical.  The mempool now wires the view in with dcrd's exact call order (`dcroxide-mempool` grew the `EnableAncestorTracking` policy, the add/remove hooks, and `MiningView`/`miningDescs`), re-using these types.  Pinned by 342 rows from dcrd's own package over a thin source mirroring exactly what the mempool provides the view: the dependency diamond with out-of-order arrival, removals with and without descendant stat updates, clone isolation, rejection, the tracking limit over a 27-chain with front removals and reversed re-adds (225 full stats/relationship states), the priority queue pop order incl. ties, and the priority float math bit for bit.  Notable dump landmine: a fresh `dcrutil.Tx` carries tree `TxTreeUnknown`, so a source that skips the mempool’s `SetTree` never forms redeemer edges.  Also ported: the template building blocks from `mining.go` — `SortParentsByVotes` (majority filtering, vote-descending order, and the top-block tie handling; Go’s sort is insertion sort at these sizes so the stable Rust sort matches), the `BlockTemplate` type, the standard coinbase/treasurybase OP_RETURN scripts (dcrd autogenerates the extra nonce; it is injected here and the vectors extract dcrd’s from its own output), `createCoinbaseTx` (the block-one ledger payout path, the pre-treasury organization/OP_TRUE outputs, the treasury-era version-3 form, and the pay-to-address/anyone-redeemable variants) and `createTreasuryBaseTx` byte for byte, `calcBlockMerkleRoot`/`calcBlockCommitmentRootV1`, `minimumMedianTime`, and `calcFeePerKb` with the negative-ancestor-stats fallback.  Pinned by 29 rows from dcrd’s own package: eleven coinbases across networks, heights, voter counts, agenda states, and subsidy split variants, four treasurybases, the parent sorting scenarios, the fee rate floats bit for bit, and the merkle/commitment roots.  Also ported: the full `NewBlockTemplate` assembly (`BlkTmplGenerator`) — the priority-queue driven mempool scan with dependency ordering, the vote/ticket/revocation stake tree handling with the vote-bits tally and too-few-voters parent recycling (`handleTooFewVoters` with the reorganization callback), the automatic revocation queue realized from dcrd's goto as a labeled loop, treasury base and treasury spend inclusion with the expenditure gate, the fee redistribution through the coinbase with the participation-scaled fees at stake validation height (`txFees` keeps dcrd's -1 placeholder overwritten with the negated total, and `txSigOpCounts` counts the coinbase twice like dcrd), block size accounting with the header overhead, both template roots over the accumulated `blockUtxos` view (whose `PrevScript` serves spent entries exactly as dcrd's viewpoint does), the median-adjusted timestamp, and `UpdateBlockTime`/`updateExtraNonce` (`maybeInsertStakeTx` and the revocation builder included).  The chain callbacks live behind a `TemplateChain` trait mirroring dcrd's mining config closures, and the transaction source behind `TemplateTxSource`, implemented by the mempool's `TxPool`.  Pinned by a 53-row scripted session against dcrd's own mining harness comparing nine template calls byte for byte (five templates, four injected errors): fee redistribution below stake validation height, both payment address variants, the error injection paths, an SVH template with four votes and a fresh ticket purchase over a mirrored fake chain, and the too-few-voters parent recycle — with dcrd's wall clock and random extra nonces recovered from its own emitted templates.  Notable dump landmine: dcrd's test harness yields mining descriptors in Go map order, which leaks into the heap insertion order and the pop order of equal-key votes, so the dump pins a deterministic hash-sorted order mirrored by the replay.  Also ported: the background template generator's regeneration state machine (`bgblktmplgenerator`) — the regen handler state with its timers modeled as armed flags whose real countdowns the daemon drives, the block connect/disconnect/accept, vote, template-update, and force-regen event handlers with the reorg bracket and the unsynced gating, the minimum-votes lock-in ladder with the max-votes propagation timeout (a vote below the maximum with the timeout armed is deliberately a no-op), side chain vote tracking with vote-driven and timeout-driven forced reorganizations (a failed reorg candidate falls through to monitoring, and with no template yet one is generated on the parent), the notified-parents LRU of size 3 with the new-votes→new-parent first-notification upgrade, and the stale-template wait counting.  Generation launches are recorded as requests the daemon executes (`genTemplateAsync` is inherently asynchronous) with completions flowing back through `process_generated_template`, and the timer select arms are exposed as `handle_*_timeout` functions for the daemon loop.  Pinned by 28 state rows from dcrd's own package driving its real handlers over a scripted chain and transaction source with generation failing fast through a scripted policy error, across eleven scenarios compared field for field.  Notable dump landmines: `dcrutil.NewBlock` caches the block hash eagerly, so sibling variants must bake distinct nonces into the header before construction (a nonce set afterward silently leaves the hash — and the scenario — degenerate); the reorg-started path clears the template synchronously through the unbuffered regen event queue, so an in-package driver must drain concurrently or deadlock; and the dump blocks' zero `time.Time` serializes as `uint32(-62135596800)` in the header codec, which participates in their hashes.  Not ported (daemon wiring): the concurrency shell — the regen queue and subscription fan-out goroutines, the real timers, and the blocking template retrieval waits |
| `mixing`, `mixing/mixpool`, `mixing/utxoproof` | `dcroxide-mixing` | vectors | The mixing library primitives — the `Message` interface as a `MixMessage` trait over the eight wire messages, message signing and verification (the signed-data BLAKE-256 digest fed into the tagged, comma-joined `schnorrHash` with the pair request's zero session and run, then EC-Schnorr-DCRv0), session ID derivation and pair request session sorting (the lexicographic pre-sort, the epoch-tagged digest, and the sid-XOR re-sort into unmixed positions) with key exchange session validation and its two error identities, the DC-net math over F = 2**127-1 (`SRMixPads`/`SRMix`/`AddVectors`/`Coefficients`/`IsRoot`, `DCMixPads`/`DCMix`, the message vectors with XOR combining) carried as canonical residues in a Mersenne-folding u128 field — dcrd computes over unreduced `math/big` integers with a final `Mod(F)`, and every dcrd-observable output is that canonical residue — the per-run ChaCha20 PRNG, the UTXO ownership proofs (`utxoproof`), `MaxExpiry`, and the mixing limits/flags/script class constants.  Pinned by a scripted session generated inside dcrd's own mixing package: the identity hash and signed-data digest of all eight messages, deterministic RFC6979 signatures byte for byte with verification and a tampering rejection, session sorting and validation across its error cases, a full honest 4-peer exponential and XOR DC-net run (pads, mixes, power sums, coefficients, and root checks with recovery of every peer message), field membership, PRNG keystreams, ownership proofs both directions, and expiries across three networks.  Also ported: `mixpool` — the in-memory pool of mixing messages with the full acceptance gauntlet (signature verification, the pair request battery: change dust/version/script checks, missing UTXOs, message counts, mix dust, total amounts, the expiry window, script class, the fee band, and the UTXO checks over an optional fetcher trait incl. duplicate outpoints, unsupported P2SH, spent/unconfirmed/wrong-version outputs, the opcode-dispatched ownership proof over regular and stake script kinds, and the summed value match; key exchange validation with session checks, position bounds, the early-epoch gate, own-PR-missing orphaning with `MissingOwnPRError`, identity and pairing compatibility; the per-type message limits with dcrd's exact messages incl. the CT limit message that prints the ciphertext count where the maximum belongs; conflict detection; and orphan handling with reconsideration cascades and source-based eviction), sessions with expiry (QK-0002: dcrd creates every session with the maximum expiry, so sessions only die through pair request expiry; ported bug for bug), the removal surface (rejected messages, sessions, confirmed sessions and mixes, spent and unresponsive pair requests) backed by the recently-removed LRU cache from dcroxide-containers, the pairing queries (`MixPRs`/`CompatiblePRs` with the expiry-ordered double-spend filter/`ReceiveKEsByPairing`), the receive collection semantics (QK-0003: dcrd's capacity-misuse error path leaks its read lock; the synchronous port returns the error), and the misbehavior observer (`CheckPrevEpoch` with the timeout blame analysis, strike sets with common-ownership merging, `MisbehavingTx`/`MisbehavingBlock`/`ExcludePRs`, and strike removal through completed mixes).  dcrd's mutexes, blocking `Receive` broadcast, background expiry task, and observer epoch ticker are daemon-phase concurrency: the port is synchronous with an injectable clock, `receive` collects what is currently accepted (dcrd's pre-cancelled-context path, which its own observer uses), and the scheduled expiry latch is exposed for the daemon to drive.  Pinned by a 1027-row scripted session generated inside dcrd's own package over mock chains with and without UTXO fetching: 110 acceptances spanning every rule rejection and orphan cascade, expiry rounds, all removals, the pairing and receive queries, and two observer strike rounds against a timing-out peer in an 8-peer pairing — comparing the full pool state and strike table after every operation.  Not yet ported: `keyagreement.go` (X25519-style ECDH + the sntrup4591761 post-quantum KEM, used only by the wallet-side `mixclient` and deferred with it) |
| `addrmgr` / `connmgr` / `peer` | `dcroxide-addrmgr` / `-connmgr` / `-peer` | vectors | Started: `addrmgr` is complete — network address types with dcrd's key and group key formatting (Go's `net.IP.String` semantics incl. the dotted rendering of IPv4-mapped forms and RFC5952 IPv6, `JoinHostPort` bracketing, the /16 IPv4 and /32 (/36 he.net) IPv6 groups with the tunnel extractions for 6to4/Teredo/RFC6052/RFC6145), the full RFC-range classification and routability rules, the reachability lattice and external-address candidacy, known-address viability (`chance` bit for bit and `isBad` across all five criteria), the manager itself (new/tried buckets over the double-BLAKE-256 bucket derivations, add with the 2N re-add likelihood, bad-then-oldest new-bucket expiry, attempt/connected/good transitions incl. the tried-bucket eviction that swaps the oldest tried address back into a new bucket, the address cache with its 23%-capped random subset, local address bookkeeping with priority scoring and best-local selection), and the `peers.json` serialization via serde with dcrd's exact field names — incl. QK-0004: dcrd never restores the serialized address timestamps on load, so loaded addresses are re-stamped with the load time (Go's zero time for the attempt/success fields round-trips exactly through its `Unix()` encoding, modeled as an optional).  dcrd's mutexes and the peers-file ticker goroutine are daemon-phase concurrency: the port is synchronous, and both the clock and the random source are injectable traits (dcrd's `crypto/rand` calls are not reproducible even in-package, so the dump pins every deterministic surface under a pinned bucket key and a lowered tried bucket size, and the randomized selection paths are covered natively with a scripted RNG).  The interior sharing is `Arc`/`Mutex` rather than `Rc`/`RefCell` — the clock is `Arc<dyn Fn + Send + Sync>`, the random source `Arc<Mutex<dyn AddrRng + Send>>`, and each shared known address `Arc<Mutex<KnownAddress>>` — so the manager is `Send` and can live in the daemon's shared server state across the peer threads (a compile-time assertion pins it); this is thread-safety plumbing with no behavioral change, confirmed by the unchanged vectors.  Pinned by a scripted session generated inside dcrd's own package: 27-address key/group/routability grids, the reachability and candidacy lattice, bucket derivations under the pinned key, manager state transitions through adds, attempts, connections, and good promotions incl. the eviction path with full per-address state comparison after every operation, viability values bit for bit through crafted serialized state, cross-loading dcrd's own `peers.json` output plus a save/load round trip, and the deserialization error cases.  `connmgr` is also complete in `dcroxide-connmgr` — the dynamic ban score (persistent + exponentially decaying transient components with the 60s half-life, 1800s lifetime, and `uint32` truncation/wrapping semantics) over a bit-exact port of Go's portable `math.Exp`/`math.Ldexp` — incl. QK-0006: Go dispatches `Exp` to platform assembly that differs from the portable code by one ulp on 276 of the 1801 possible decay ages, so dcrd's ban scores are already platform-dependent and the port follows the portable source; the connection manager itself as a synchronous state machine (pending/established tracking, the cancel-while-pending semantics where a late success is closed and ignored, permanent-retry backoff of `retryCount × RetryDuration` capped at 5 minutes, the 25-failure network-outage threshold that defers new connection churn, target-outbound autoconnect, `CancelPending`/`ForEachConnReq`, and the config validation errors) with dialing through caller-provided closures and timer arms returned as events; the Tor SOCKS DNS resolver over an injectable transport (exact request bytes incl. the two zero port bytes Go's buffer arithmetic produces, the full status-code error table, and the unvalidated variable-length IPv6 reply dcrd accepts as-is); and HTTPS seeding over an injectable transport (dcrd's exact request URL with sorted query filters, the byte-limited streaming JSON decode capped at 16 nodes with Go's decoder error texts including the struct-field context, and the SplitHostPort/ParseUint/ParseIP host acceptance chain).  dcrd's handler goroutine, channels, `time.After` retries, and listener plumbing are daemon-phase concurrency.  Pinned by a scripted session generated inside dcrd's own package: the whole 1801-value decay domain bit for bit, a ban score battery replaying dcrd's own methods on platform-agreeing ages, scripted SOCKS exchanges against a local fake proxy comparing the request bytes and every error path, the seeder host acceptance matrix, the streaming JSON parse battery incl. the byte-limit truncation, and the request URLs — plus a native scenario battery mirroring dcrd's `connmanager_test.go`.  The protocol decision core of `peer` is also complete in `dcroxide-peer` — version negotiation over an injectable message transport (the first-message-must-be-version rule, self-connection detection against the shared sent-nonce cache, the protocol minimum negotiation, the state updates that happen even when the peer is then rejected for advertising less than the required version, and the exact error texts), local version construction (the services-only local address, the proxy-address hiding incl. the invalid-proxy safe side, the user agent assembly with QK-0008: dcrd ignores the error from an invalid configured agent and silently advertises only the wire default, and the newest-block hook), the push builders (getblocks/getheaders with their back-to-back duplicate filters and dcrd's exact locator encodings, addr with the over-limit shuffle-and-truncate through an injectable source), ping/pong statistics, the known-inventory cache over the containers LRU with dcrd's size and TTL, the stall deadline table, connection-address conversion, the outbound constructor's address handling, and the configuration defaults.  dcrd's input/output/queue/stall handler goroutines, connection association, and the SOCKS dialing are daemon-phase concurrency; the state dcrd keeps in package globals (the id counter and sent nonces) lives in an explicit shared value.  Pinned by a scripted session generated inside dcrd's own package over real piped connections: thirteen negotiations comparing the verdicts, error texts, resulting peer state, and the exact framed local version bytes with only the nonce and timestamp normalized (dcrd cannot inject its randomness), the push builder sequences byte for byte incl. the duplicate filters, the addr limit behavior, ping/pong nonce handling, the full deadline table, and the address conversion and outbound constructor batteries |
| `internal/netsync` | `dcroxide-netsync` | vectors | Complete as a synchronous decision core: the `SyncManager` with all of its handler surface (`OnPeerConnected`/`OnPeerDisconnected` with sync peer selection and the in-flight re-request sweep, `OnHeaders` with the whole orphan-announcement/consecutive-orphan/min-known-work/disconnect matrix and the initial header sync completion transition, `OnBlock` with the best-header rejection recovery (sync height reset plus header re-requests to every data-serving peer), `OnInv`/`OnNotFound`/`OnTx`/`OnMixMsg`, `RequestFromPeer`/`RequestMixMsgFromPeer`, `ProcessBlock`, and the header sync stall handling), the netsync `Peer` state (serves-data flag, consecutive orphan counter, announced-orphan and best-announced-block tracking with cumulative work comparison, the once-only initial state request choosing getminingstate or getinitstate by protocol version, plus the embedded peer facts it reads: last block height with monotonic updates, the known-inventory cache, and the getheaders duplicate-request filter), the next-needed-blocks lookahead buffer with dcrd's exact refresh conditions, the rejected transaction/mix APBFs with the on-block reset, `limitAdd`, and the header sync progress estimators.  Per the sync-port doctrine the handlers return `Action` values (queue message, disconnect, stall-timer arm/stop) instead of calling into peer objects, the stall timer fires through an explicit `on_header_sync_stall_timeout` entry point, and the chain/mempool/mixpool sit behind `SyncChain`/`SyncTxPool`/`SyncMixPool` traits; `Config.MaxPeers` is unused by dcrd's manager logic and has no equivalent; the periodic progress logging is not ported (log-only).  dcrd keys its peer set and request maps by pointer with random map iteration, so sync-peer ties, replacement-announcer choice, and re-request entry order are arbitrary there — iteration here is deterministic (documented at each site) and the dump avoids the ambiguous cases.  `internal/blockchain` gained `PutNextNeededBlocks` (the sliding-window walk over the index).  Pinned by an 1129-row scripted session generated inside dcrd's own package: a real `SyncManager` over a real chain fed the sequential accepted prefix of dcrd's own fullblocktests blocks, real negotiated peers over synchronous pipes with every received message recorded byte for byte, and a real mempool/mixpool — 87 steps covering the full initial header+chain sync (including the getdata batching against the 16-block in-flight cap and refill at 10), steady-state announcements, orphan announcement tracking with resolution, the consecutive-orphan disconnect at 10, unrequested-block and invalid-ancestor-header disconnects, the corrupted-best-header-block recovery, disconnect re-requests from alternate announcers, the rejected-filter lifecycle across a new block, recently-confirmed suppression, notfound wrong-peer/right-peer handling, mix request/reject flows, and the initial state requests on chain sync completion — replayed through the REAL Rust chain engine with scripted transaction/mixing pools (each pinned by its own piece), comparing all 25 queued messages, every disconnect via the peer state probes, and 949 state probes (sync peer/height, is-current, request maps, per-peer tracking state, best header, rejected filters, and `PutNextNeededBlocks` outputs), plus the float-bit-exact header sync progress batteries |
| `internal/rpcserver`, `rpc/jsonrpc/types`, `dcrjson` | `dcroxide-rpc`, `dcroxide-dcrjson`, `dcroxide-rpctypes` | vectors | Started: `dcrjson/v4` is complete — dcrd drives the whole package through Go reflection over registered command struct types, so the port makes the reflection data explicit as `GoType`/`GoValue` descriptor trees (struct fields with their `json`/`jsonrpcdefault`/`jsonrpcusage` tags, pointers-as-optionals, and Go's type display strings for the error messages) and reimplements Go `encoding/json` semantics over them: HTML-escaped strings (`<`/`>`/`&` to `<` etc. plus U+2028/9), Go's shortest-round-trip float formatting with the e-form cutoffs (`abs < 1e-6 || >= 1e21` for JSON with the `e-0X` cleanup; decimal exponent `< -4 || >= 6` for `%v` in usage/help text), bytewise-sorted map keys, declaration-order struct fields with `omitempty`, `[]byte` as base64, and a Go-faithful JSON validator/decoder producing `json.UnmarshalTypeError` values (`number 3.14`, leaf types from nested failures) and scanner syntax messages (`unexpected end of JSON input`, `invalid character 'x' ...`) byte for byte.  On that base: the registry (`Register` with the full validation gauntlet and its error strings, usage-flag battery incl. the `String()` rendering with unknown-bit hex tails, `(dynamic type, string)` method keying so a plain-string lookup of a named-type method correctly misses, `RegisteredMethods` filtering), `MarshalCmd` (positional params cut at the first nil optional), `ParseParams` (defaults populated only for absent trailing params — an explicit `null` leaves the field nil with no default, pinned), `NewCmd` with the whole `assignField` conversion matrix (numeric widths with Go's overflow checks, `ParseInt` base-0 semantics incl. hex, `ParseBool`/`ParseFloat` acceptance, string-to-compound via JSON), `MethodUsageText` (incl. the English plural-to-singular squashing that turns `names` into `"nam"`), `GenerateHelp` over result-type descriptors with a port of Go's `text/tabwriter` column-block algorithm (per-block widths, trailing cells unpadded — the help output incl. the misaligned map-entry lines matches byte for byte), the JSON-RPC `Request`/`Response`/`RPCError` marshalling with all the error-code constants, and the concatenated-hashes helpers.  Go's package-global registry mutex is daemon-phase concurrency; the port keeps an explicit `Registry` value, and the pointer-helper functions have no equivalent because optionals are `GoValue::Null`.  Pinned by a 217-row scripted session generated inside dcrd's own package: the usage-flag stringer, 39 registrations spanning every validation error, request/response bytes across id types and versions, 15 command marshals covering the float/escaping/map-order surfaces, all 50 `NewCmd` conversions and failures, 34 `ParseParams` rows covering defaults, nulls, every type-error message shape, and the syntax-error family, usage text for every registered shape, and full generated help for twelve method/result combinations incl. the missing-description partial-output path.  `rpc/jsonrpc/types/v4` is also complete — all 105 registered methods (77 chain server commands, 17 websocket-only commands, 11 websocket notifications) plus every command sub-struct and result struct expressed as descriptor functions over the dcrjson base, with the registrations performed in dcrd's `init` order under the `types.Method` dynamic type, the named string constants (`AddNodeSubCmd`/`NodeSubCmd`/`EstimateSmartFeeMode`/`GetRawMempoolTxTypeCmd` values, agenda statuses, and notification method names), and a port of `Vin`'s custom `MarshalJSON` five-shape switch (coinbase/stakebase/treasurybase/treasury-spend/regular) producing raw JSON embedded through the new `GoValue::Raw` escape, which mirrors Go's `json.Marshaler` semantics in the dcrjson encoder.  A related Go artifact is preserved knowingly: the custom marshaler has a pointer receiver, so a `Vin` marshalled directly as a non-addressable top-level value renders the plain twelve-field struct — dcrd only ever marshals vins through slices and pointers, where the custom shapes apply.  The Go `New*Cmd` constructor helpers have no equivalent because command instances are built directly as `CmdInstance` values.  Pinned by a 472-row scripted session generated inside dcrd's own types package: the full sorted method list, per-method usage flags and one-line usage text for all 105 methods (field names, order, optionality, defaults, and usage tags), a zero-value marshal of every one of the 171 struct types (json tag names, declaration order, omitempty behavior, and base encodings byte for byte), 26 curated populated command marshals covering maps, nested structs, optional-tail truncation, notifications with nil ids, and a fully-populated raw transaction with all five vin shapes, 14 parameter-parsing default round trips, 10 populated result marshals, the six vin shape rows, spot help generation over three verbose result descriptors, and the 31 string constants.  From `internal/rpcserver`, the help subsystem is also complete in `dcroxide-rpc`: the 634-entry English help description map (generated from dcrd's `helpDescsEnUS` at the tag and behaviorally verified through the pinned help output), the per-method result type table (`rpcResultTypes`, 93 methods), the HTTP and websocket handler method sets that drive the usage listing, and the caching `helpCacher` — incl. QK-0005: the usage cache holds a single string regardless of the websocket flag, so whichever transport requests usage first fixes the text both serve.  Pinned by a scripted session generated inside dcrd's own internal/rpcserver package: the complete generated help text for all 93 methods byte for byte (exercising the dcrjson help machinery, the tabwriter, and every result descriptor end to end), the handler method sets, the no-result-types error text, and the one-line usage in both variants plus the poisoned-cache row.  The pure transform layer under the handlers is also complete: the RPC error constructors (`rpcInternalErr` through `rpcMiscError` with dcrd's exact codes and message shapes, incl. the `%q`-quoted hex-decode error), `normalizeAddress` with the interface-name substitution behind an injectable lookup (IPv6 zone appending on the link-local `fe80::/10` unicast and `ff02::/16` multicast checks), `decodeHashes` (full-length, non-reversed) versus `decodeHashPointers` (`NewHashFromStr`, incl. its short-string padding acceptance), `getDifficultyRatio` implemented as Go `big.Rat.FloatString(8)` integer math (round half away from zero, then re-parse as a float) so every result bit pattern matches, `thresholdStateToAgendaStatus` with unknown states mapping to `defined`, `convertVersionMap`, `bigToLEUint256` padding/truncation, the getwork data helpers (`getWorkTemplateKey`, `serializeGetWorkData` with the computed blake256 internal padding and the zero blake3 padding, and both length constants), and `createVinList`/`createVoutList`/`createTxRawResult`: the treasurybase-before-coinbase classification order, dcrd's pre-sized vin lists leaving zero-valued entries on the single-input shapes, sstx odd-output commitment address/amount decoding, the address filter, and the header timestamp duplicated into both `time` and `blocktime`.  Pinned by a 52-row dump generated inside dcrd's own package: fully marshalled `createTxRawResult` JSON for all six transaction shapes (regular incl. an unparseable script, coinbase, crafted treasurybase and treasury spend self-verified against dcrd's own classifiers, ticket, vote) with and without a header and with treasury voting off, the hash-mismatch error text, vout filtering, seven difficulty-ratio bit patterns across two networks, the hash decode batteries incl. the short-string divergence between the two decoders, the agenda status and version-map rows, `bigToLEUint256` edges incl. a >256-bit truncation, both getwork serializations, and all thirteen error constructors.  The first command handler slice is also ported into a `Server`/`Config` scaffold whose chain sits behind an `RpcChain` trait (best snapshot, header by height, and the treasury/subsidy-split agenda queries — the seam grows with each slice): `createrawtransaction` and `createrawsstx` (input/output validation with dcrd's exact error strings incl. the Go `%v` float rendering of `dcrutil.MaxAmount` as `2.1e+15`, `dcrutil.NewAmount` rounding, the null block index sentinel from `wire.NewTxIn`, stake address gating with Go type names in the messages, and the commitment/change construction over `SStxNullOutputAmounts`/`CheckSStx`), `decoderawtransaction` (odd-length hex padding, Go's `unexpected EOF` truncation text, and the piece-70 vin/vout builders), `decodescript` (pubkey-to-P2PKH address rendering, the P2SH form omitted for script-hash scripts, and the version-generic script hash error text), `estimatefee`, `getblocksubsidy` (agenda-driven subsidy split variant selection over the subsidy cache), `getcurrentnet`, `validateaddress` (undecodable addresses yield the invalid result with no error), and `verifymessage` (a port of `ecdsa.RecoverCompact` added to dcroxide-dcrec over libsecp256k1 recovery, plus a Go-exact `encoding/base64` StdEncoding decoder whose corrupt-input offsets surface in the RPC error text).  dcrd iterates the amounts maps of the create commands in random Go map order, so the output order with multiple entries is arbitrary there; entries are processed in JSON order here and the vectors use single-entry maps.  Pinned by a 61-case dump generated inside dcrd's own package running the real handlers over its own mock chain: each case carries the marshalled request params (parsed in the replay through the ported dcrjson `ParseParams` pipeline end to end), the mock seam facts, and the marshalled result or exact error code and message byte for byte.  The chain-query handler slice follows over a grown `RpcChain` seam (best snapshot/header, block/header/hash lookups, chain work, chain tips, median time, max block size, locate headers, threshold state and state-changed height, the blake3 pow agenda, and is-current) plus an `RpcSyncManager` seam: `getbestblock`, `getbestblockhash`, `getblockcount`, `getcoinsupply`, `getdifficulty`, `getblockhash` (with the out-of-range error), `getchaintips`, `getblockchaininfo` (the verification progress ratio, the genesis special case for max block size and agenda defaults, and the full deployments map whose agenda entries carry the threshold status and state-changed heights), `getheaders` (the piece-70 hash decoding plus the exact hashstop error text), and `getblock`/`getblockheader` in all verbosity modes (the pow hash selected by the blake3 agenda, next-hash/confirmations only for main chain blocks, the difficulty ratios, `%064x` chain work, and the verbose-tx path building every transaction through the piece-70 raw result builders) — note `getblock` renders the parsed (normalized) hash into its not-found error while `getblockheader` echoes the request string, both preserved.  Pinned by a 41-case dump generated inside dcrd's own package over its own mock chain seeded with mainnet block 432100, byte-comparing every marshalled result (incl. the full verbose block with all seven real transactions) and error.  The stake-query handler slice follows (the seam gains the stake difficulty estimator, live ticket checks, want-height, stake versions, vote info/counts, ticket pool value, and the treasury balance with its unknown-block/no-treasury error classifications): `estimatestakediff` (the fresh-stake averaging walk over the retarget window), `existsliveticket`, `existslivetickets` (Go's LSB-first `bitset.NewBytes` flag packing to hex), `getstakedifficulty` (the difficulty error code with the chained error text), `getstakeversioninfo` (the interval walk with its off-by-one adjust and count clamping) and `getstakeversions`, `getvoteinfo` (quorum and per-choice progress only for started agendas, and the unrecognized-vote-version error), `getticketpoolvalue`, `gettreasurybalance` (tip default, verbose updates, and the block-not-found/treasury-inactive error split), and `livetickets`.  Pinned by a 41-case dump generated inside dcrd's own package over its own mock chain (with the real mainnet version-9 deployment agendas flowing through the vote info results), byte-comparing every marshalled result and error.  The mempool and connection handler slice follows over new `RpcConnManager`/`RpcTxMempooler`/`RpcClock` seams: `addnode` and `node` (address normalization through the piece-70 helper with the injectable interface lookup, Go-strict `ParseUint` node ids, the `SplitHostPort`-or-`ParseIP` target classification, the capitalization split between the disconnect and remove invalid-target messages, and the permanent/temporary peer hints keyed off `peerExists`), `getconnectioncount`, `getnettotals` (the injected clock's millisecond time), `ping` (broadcast with an injected nonce source), `getmempoolinfo`, `getrawmempool` (the type filter table with the exact supported-types error text, and the verbose map keyed by transaction hash), and `existsmempooltxs` (reversed-hash decoding and the bit-flag packing).  Pinned by a 34-case dump generated inside dcrd's own package over its own connection manager and mempool mocks with crafted pool descriptors, byte-comparing every marshalled result and error.  The tx/utxo lookup slice follows over new `RpcTxIndexer`/`RpcDb`/`RpcFiltererV2` seams plus utxo queries on the chain seam: `getrawtransaction` (the mempool-first lookup, the tx-index requirement and its exact configuration error, the six-block lag and single-shot sync-wait gauntlet with dcrd's three-second timeout modeled behind the seam, raw region bytes returned verbatim in the non-verbose database path, and the mempool path treating the best tip as the previous block), `gettxout` (the mempool tree mismatch falling through to the utxo set, the invalid-vout error, JSON null for missing or spent outputs, and confirmations from the entry height), `gettxoutsetinfo` (dcrd returns the bare stats error which its dispatch layer wraps; the wrapped internal error stands in until the dispatch layer is ported), and `getcfilterv2` (the no-filter/block-not-found mapping; dcrd allocates a zero-length proof-hash slice and assigns into it by index, which would panic for any non-empty proof — unreachable since the header commitment has a single leaf, and the panic is mirrored deliberately).  Pinned by a 28-case dump generated inside dcrd's own package over its own mocks (including dcrd's real block 432100 filter bytes), byte-comparing every marshalled result and error.  The peer/address slice follows (the connection manager seam grows rich peer stats plus persistent peers and DNS lookup, and an `RpcExistsAddresser` seam joins): `getpeerinfo` (the `%08d` services rendering, ping wait only for outstanding pings through the injected clock, and the id-sorted output), `getaddednodeinfo` (the last-match node filter dcrd's reslice-under-range produces, the plain and DNS forms, and the lookup fallback to the bare host), `existsaddress`/`existsaddresses` (the disabled-index error, the same sync gauntlet as the tx index, the invalid-parameter query errors, and the bit-flag packing), and `ticketsforaddress` (invalid-parameter decode errors unlike the address-key errors elsewhere, and the stake-address gate).  Pinned by a 24-case dump generated inside dcrd's own package over its own mocks with full peer stats snapshots, byte-comparing every marshalled result and error.  The submission/control slice follows (the sync manager seam gains transaction relay with its rule/duplicate classification, recently-confirmed checks, and block submission; log manager, smart fee estimator, and optional block templater seams join): `sendrawtransaction` (the rule-error ladder rendering `rejected transaction <hash>: <err>` with the duplicate code for known duplicates or recently confirmed transactions, the deserialization code wrapping non-rule failures as `rejected: failed to process transaction ...`, relay of accepted transactions, and rebroadcast tracking for everything but votes; the websocket notification hook arrives with the websocket layer), `submitblock` (rejections returned as a string result rather than an error, and Go's exact `encoding/hex` error text surfacing through the internal error), `invalidateblock`/`reconsiderblock` (the unknown-block and invalidate-genesis mappings and the reconsider-failure code over dcrd's all-rule-errors walk), `regentemplate` (the not-configured-for-mining error), `debuglevel` (the show listing and the invalid-level error), and `estimatesmartfee` (conservative-only mode gate).  Pinned by a 30-case dump generated inside dcrd's own package over its own mocks, byte-comparing every marshalled result and error.  The fee-info/node-info slice follows (the chain seam gains block-by-height, height-range, and auto-revocations-agenda queries; sanity checker and median time source seams join): `createrawssrtx` (revocation construction over the ticket utxo minimal outputs — the P2SH commitment path, the automatic ticket revocations version-2/zero-fee path, and the full error ladder from input shape through `Invalid Tx type: <n>` to the ticket-submission amount check rendered with Go's `Amount.String()`), `ticketfeeinfo`/`txfeeinfo`/`ticketvwap` (the fee statistics — dcrd's `min`/`max`/`mean`/`median`/`stdDev` including the zero-seeded max, integer-division mean, and float-domain stddev with amount rounding — over per-block fees sized by the header's type count and per-KB fraud-proof fees, plus the stake-difficulty-window walk and the VWAP header scan with its height-range gates), `verifychain` (chain and sanity errors folded into the boolean result; requests parsed with the jsonrpcdefault 3/288 exactly like dcrd's dispatch), `getinfo` (the version integer, the truncated time-source offset seconds, and the proxy/testnet/txindex configuration surface), and `version` (the ported `internal/version` constants — 2.1.5+release.local — with the normalized runtime version folded into the build metadata, keyed alongside the 8.3.0 JSON-RPC API semver).  Pinned by a 39-case dump generated inside dcrd's own package over its own mocks and dcrd's real mainnet block 432,100, byte-comparing every marshalled result and error; the dump routes every request through `dcrjson.ParseParams` so defaults apply as in live dispatch.  The mining/network/mix slice follows (CPU miner, mix pooler, profiler manager, and address manager seams join; the sync manager gains mix message acceptance and the connection manager mix relay): `generate` (the no-mining-address and generate-unsupported gates — the latter rendering the currency net name — plus the cancellation and connection-closed classifications; the context-cancellation path is unreachable in the dump and follows the source), `getgenerate`/`gethashespersec`/`setgenerate` (the zero-worker-limit override and the mining-address gate), `getnetworkhashps` (the big-int work summation between the min and max header timestamps with the first block excluded from the work total, the zero returns for invalid heights, and Go's low-64-bit `big.Int.Int64` on the quotient), `getmininginfo` (nesting the getnetworkhashps handler with a nil-field command exactly as dcrd does), `getnetworkinfo` (local addresses, the per-network reachability table, and `%016x` local services), `getmixmessage`/`getmixpairrequests` (mix payloads re-encoded at the mix protocol version), `sendrawmixmessage` (the eight wire command gates and dcrd's lazy hex-decoder semantics: the message decodes from the valid prefix, hex problems past the end of the message are never observed, truncation surfaces `unexpected EOF`, and a bad byte surfaces Go's exact `encoding/hex` text; the websocket notify hook arrives with the websocket layer), `startprofiler`/`stopprofiler` (the profiler-state error code paths), and `stop`.  Pinned by a 44-case dump generated inside dcrd's own package over its own mocks plus crafted pair request and key exchange fixtures.  The treasury spend votes handler follows (the chain seam gains mined-tspend lookup and vote counting; the mempool seam gains the tspend hash listing): `gettreasuryspendvotes` — the mempool tally over all pool tspends, client-specified tspends resolved from the mempool (with the is-a-tspend sanity gate) or from mined blocks via the stake tree, the main-chain end-block walk that stops the count at the block before a mined tspend, zero tallies outside the voting window with the window bounds still reported, the wrapping `expiry-2` treasury-vote-interval check, and the unknown-block/internal vote count classifications (dcrd dereferences a nil error when a fetched block lacks the tspend; the port panics on that unreachable path).  Pinned by a 19-case dump generated inside dcrd's own package over its own mocks.  The getwork handler and its work state follow (the block templater seam gains the current template, subscription, and block-time update surface; the sync manager's block submission gains the rule-error classification): `getwork` — the CPU-mining/mining-address/connectivity/sync gates (with the unsynced-mining and initial-chain-state exceptions), the template pool keyed by the merkle and stake root pair with height-based pruning, the tip-change wait that picks an updated template off the subscription (falling back to the current template after dcrd's 5.5-second known-template timeout, which surfaces the chain-reorganization error when the template has gone away), the serialized work data with the blake256 internal padding or the blake3 zero padding chosen by the agenda, the little-endian byte-swapped target, and the submission path (front-padded odd-length hex, the length and hex gates, orphan and proof-of-work rejections as false results with the agenda-chosen pow hash, template reconstruction, and rule rejections as false versus non-rule failures as internal errors).  dcrd's single-item work semaphore and request-context cancellations are concurrency machinery with no synchronous counterpart; the seams model the cancellation outcomes.  Pinned by a 21-scenario dump generated inside dcrd's own package whose steps share one server so the work state carries across calls (including two real 5.5-second timeout waits).  The dispatch core follows: `parseCmd` (the method-not-found/wallet/unimplemented classification over the `rpcAskWallet`/`rpcUnimplemented` method sets and the failed-to-parse conversion of parameter errors), `standardCmdResult` over the complete 77-method handler map (each arm pairing its handler with the Go result type that drives the JSON encoding, including the dual-shape verbose and getwork results), `createMarshalledReply`, and `processRequest` (the `rpcLimited` gate for non-admin users, the malformed empty-method reply, and JSON-RPC notification suppression), plus the `help` handler over the previously ported help cacher — completing all 77 dcrd command handlers.  Pinned by a 19-case dump driving dcrd's own `processRequest` end-to-end and comparing every reply byte for byte, the full help usage overview included.  The auth checks and request-body processing follow: `checkAuth`/`checkAuthMAC`/`checkAuthUserPass` (Basic auth decisions over HMAC-SHA256'd credential strings under a per-server random key, the constant-time double comparison against the admin and limited credentials, and the no-credentials TLS mode that always passes), and the body handling inside `jsonRPCRead` (Go-faithful request unmarshalling with `encoding/json`'s exact error texts including case-insensitive and duplicate JSON keys, the failed-to-parse and invalid-request replies at their respective codes and versions, single and batched requests with the empty-batch error and per-entry errors, notification suppression, invalid response ids dropping the reply entirely, and the response assembly with the Bitcoin Core compatibility newline).  Pinned by a dump running dcrd's own `checkAuth` (9 decisions) and `jsonRPCRead` over a real HTTP test server (20 full response bodies compared byte for byte).  The websocket client core follows (`rpcwebsocket.go`'s synchronous surface): the per-client transaction filter with its typed fast paths (pubkey hashes, script hashes, compressed pubkeys) plus the fallback string map and unspent outpoint set, block rescanning (the stakebase input skip for votes, the coinbase classification under the active treasury agenda, and discovered outputs feeding the outpoint set), and all 17 websocket command handlers — loadtxfilter (reload versus incremental merge and the raw hex outpoint error), rescan (the filter-required, block-not-found, and non-child gates), session, the notification registrations behind a manager seam, rebroadcastwinners over the tip generation, and the websocket help variant over the combined method lists — with the service routing that falls back to the standard dispatch.  The connection shell (the in/out/queue handler goroutines and the notification fan-out) has no synchronous counterpart.  Pinned by a 19-scenario dump generated inside dcrd's own package whose steps share one client so the filter state carries across calls, discovering transactions from real mainnet block 432,100.  The websocket notification builders follow: `subscribedClients` (per-client relevance over the filters, including the ticket commitment address path where OP_RETURN commitments resolve to their embedded addresses without watching the unspendable output) and the payload construction for every notification — blockconnected with per-client relevant transactions, blockdisconnected, work (the three template update reason strings plus the template pool insert and new-parent pruning), tspend, reorganization, winningtickets (the ticket map keyed by decimal indexes), newtickets, txaccepted and the shared txacceptedverbose payload over the full raw transaction result, relevanttxaccepted with output watching, and mixmessage — each marshalled exactly like dcrd's `MarshalCmd("1.0", nil, ntfn)` requests and returned per client for the daemon's fan-out.  Pinned by a 14-event dump driving dcrd's own builders over clients with buffered channels and comparing every drained notification byte for byte.  Not yet ported from `internal/rpcserver`: the live HTTP/websocket connection shells, which land with the daemon (Phase 13 essentially complete) |
| `rpcclient` | `dcroxide-rpcclient` | — | Phase 13 |
| `container/apbf`, `container/lru` | `dcroxide-containers` | vectors | Complete: the age-partitioned bloom filter (the false positive rate recursion, near-optimal K/L derivation incl. the L=100 brute-force cap, capacity/size/bits-per-slice derivations, enhanced double hashing over 128-bit SipHash-2-4 with dcrd's exact wrapping arithmetic incl. the deliberate slice-0 underflow in the closed formula, Lemire fast reduction, the ring-buffer generation aging with non-byte-aligned slice clearing, the consecutive-match query with its backward-jump optimization, and the deterministic reset key derivation) and the generic LRU map and set (recency ordering with in-place updates, eviction at the limit with element reuse — an index arena stands in for Go's pointer-linked list — default and per-item TTLs with the zero-TTL-means-100-years encoding, strictly-greater expiry, the invisibility of expired-but-unscanned items, the 30-second lazy expiration scans, immediate eviction, the hit ratio with its no-lookup 100% convention, and clearing).  dcrd's mutexes are daemon-phase concurrency; the ports are single-threaded with identical semantics, and the APBF hash keys and LRU clock are injectable for the tests (the LRU clock is `Arc<dyn Fn + Send + Sync>` so the caches inside a peer can move between the daemon's per-peer threads).  Pinned by scripted sessions generated inside dcrd's own packages: the siphash word mapping, the FP-rate grid bit for bit, construction metadata across dcrd's own parameter tables, add/probe/age/reset sessions with injected keys comparing the packed ring buffer byte for byte and the membership verdicts — false positives included — bit for bit, and an LRU battery over a mock clock comparing length, LRU-to-MRU order, eviction counts, and the hit ratio after every operation |
| `certgen` | `dcroxide-certgen` | vectors | Complete: self-signed TLS certificate pair generation for the RPC server — `NewTLSCertPair` over the two curves dcrd's `--tlscurve` accepts (P-256, the default, and P-521) and `NewEd25519TLSCertPair` — with the wall clock, key/serial randomness, hostname, and interface addresses injectable.  The certificate bytes come from a minimal DER writer reproducing Go `x509.CreateCertificate` exactly for this template shape: UTCTime validity, PrintableString/UTF8String subject selection, the critical KeyUsage and BasicConstraints extensions, the auto-computed SHA-1 SubjectKeyIdentifier Go adds for CA certificates, the SAN with hostname/localhost/interface/extra-host gathering incl. Go's `IP.Equal` deduplication and 4-byte IPv4 SAN encoding, the ASN.1-end-of-time clamping, and IDNA conversion of non-ASCII names (QK-0007: the Ed25519 variant lacks that conversion and fails on non-ASCII hostnames; ported bug for bug).  Keys marshal as RFC 5915 SEC1 (ECDSA) and PKCS#8 (Ed25519) with Go's PEM formatting.  Pinned by a scripted session mirroring dcrd's template-building lines with fixed inputs: Ed25519 certificates byte for byte incl. the deterministic signature across host/serial/clamping variants, ECDSA to-be-signed bytes and key encodings byte for byte for both curves (Go randomizes ECDSA signatures; the port's self-signature is verified over the pinned TBS instead), the IDNA row, and both error texts |
| `base58` | `dcroxide-base58` | vectors + diff | decred/base58 v1.0.6 port: modified base58 + Decred base58check (2-byte version, double-BLAKE-256 checksum), preserving the invalid-input-decodes-empty quirk; all upstream vectors + live differential over random payloads and mutated strings |
| `bech32` | `dcroxide-bech32` | — | Only needed if a future address version adopts it; nothing at the parity tag uses it |
| `dcrutil` | `dcroxide-dcrutil` | — | Phase 1–2 |
| daemon (`config`, `server`, `cmd/*`) | `dcroxide-node` | vectors | Started: the configuration pipeline from `config.go` — the config struct with every option and dcrd's defaults, the four network parameter groupings with their RPC ports (`params.go`), and the full loadConfig validation and derivation gauntlet with dcrd's exact error strings: network selection, the acceptnonstd/rejectnonstd network-default resolution, per-network data/log dir namespacing, logsize parsing (all-digit values are invalid because the unit scan never breaks), the debug level grammar over the nineteen subsystem IDs with slog's level aliases, db type validation, utxo cache clamping, profile address validation (port-only values gain 127.0.0.1 and Go's SplitHostPort error texts surface), the ban/dial/idle duration floors with Go duration formatting in the errors, whitelist CIDR/IP parsing with Go's masking and To4 semantics, addpeer/connect exclusivity with the listen/seeding side effects, default listeners, the RPC credential rules (basic-auth auto-disable, the localhost default listeners from an injected resolver, and the clientcert restrictions), localhost-only notls enforcement, minrelaytxfee amount conversion (`dcrutil.NewAmount` incl. the NaN rejection), block size bounds against the network maximum, index option conflicts, mining address decoding through the ported stdaddr with matching error texts, the mainnet unsynced-mining rejection with the simnet/regnet force-enable, listener/peer address normalization with interface expansion behind an injectable lookup, Tor/proxy wiring modeled as selection enums (the dial/lookup closures are daemon wiring), the network interface states feeding getnetworkinfo, and TLS curve validation.  Also ported: `cleanAndExpandPath` over faithful ports of Go's `os.Expand` and `filepath.Clean` and the `~` handling (incl. the no-separator `~foo` quirk that joins onto the current user's home), `dcrutil.AppDataDir`, Go duration parsing/formatting, and `createDefaultConfigFile` over the embedded sample config with injected randomness.  The go-flags application-order semantics the pipeline observes are reproduced: the help pre-parse applies the command line before the config file, each parse pass resets slice options on first set — so command-line slice values wholly replace config file values while repeats within a pass accumulate — and regnet clears config-file peers only when the command line selected regnet.  Also ported: the go-flags v1.6.1 front-end over the 86-option registry (`load_config_from_argv`) — the command line scanner (long options with `=` or separate arguments, short options in separate/concatenated/`=` forms with cluster semantics, the negative-number exception for signed option types, quoted values through a `strconv.Unquote` port, `--` termination in the final parse only — so options after `--` leak through the help pre-parse into the effective config while still returning as remaining arguments, a pinned dcrd quirk — and go-flags' exact error texts: unknown flag, expected argument, got-option/got-double-dash, bool-flag-cannot-have-argument, and the `(expected TYPE)` marshal wrapper over Go's strconv/time `NumError` texts with base-0 integer parsing incl. hex/octal/underscores); the INI grammar (trimmed `key=value` lines, `;`/`#` comments, quoted values, bool empty-value and `ParseBool` forms, bare keys as malformed-line errors, case-insensitive `[Application Options]` section resolution with unknown-group errors, and go-flags' name matching where the Go struct field name and even the single-character short name match ini keys while case variants of the long name do not, with per-line `file:line:` error prefixes); and the `env` tag defaults (`DCRD_APPDATA`, `DCRD_ALT_DNSNAMES` with comma splitting) applied at the end of each successful parse for options that parser never set — including the pinned quirk that an unset appdata takes the raw environment value after the home rewrite already derived the other paths from its cleaned form, and that config-file values suppress the environment while an explicit command line always wins.  Pinned by 76 pipeline scenarios plus 52 syntax scenarios from package-main dumps driving dcrd's real loadConfig over temp home directories: every effective config field, the cooked values (fee atoms, decoded mining addresses, whitelist networks via IPNet.String, the three network info states, the selected params with its RPC port, and the per-subsystem log levels), the remaining arguments, and every error text — plus the sample config byte for byte, createDefaultConfigFile under both auth types with tokenized credentials, and a cleanAndExpandPath battery.  The help/version/debuglevel=show exits are represented as distinguished results for the daemon shell (the help text itself is not yet generated).  Also ported: the pipe IPC lifecycle protocol (`ipc.go`) — the binary message framing (protocol version 1, type-length/type, little-endian payload length) for the `lifetimeevent`, `p2plistenaddr`, and `rpclistenaddr` messages Decrediton consumes over `--pipetx`, with every message kind pinned byte for byte against dcrd's own framing (the pipe reader/writer loops and the outgoing channel are daemon runtime); and `internal/version` — `parseSemVer` matching dcrd's semver regular expression exactly (hand-rolled with the numeric no-leading-zero rules and the pre-release/build identifier grammars) incl. the exact malformed-version and `strconv.ParseUint` range error texts, `NormalizeString`, and the pinned `2.1.5+release.local` release version with its parsed components, pinned by 83 verdict rows from an in-package dump over the upstream test table plus range and separator edge cases (dcrd's `vcsCommitID` build-info fallback never runs at the tag because the release version carries explicit build metadata; not ported).  Also ported (server.go slice 1): the server's address bookkeeping — the bounded network address submission cache (`naSubmissionCache`: adds reset the score to one and stamp the injected clock, the oldest access time is evicted when the limit is reached even when re-adding an existing key, and per-type best submissions — dcrd breaks last-accessed and score ties by Go's random map iteration while iteration here is in key order, with the dump avoiding ties), `hostToNetAddress` over the address manager's host encoding with a resolver fallback (first resolved IP; "no addresses found" on empty), `ResolveLocalAddress` (the 60-percent-of-target-outbound majority ceiling, per-listener matching by exact host after IPv6 zone stripping, generic listeners, and probable-external endpoints by reachability class, adding manual-priority local addresses through the ported address manager with dcrd's abort-vs-skip error behavior), the wire/address-manager net address conversions, and the `hasServices`/v1-address-type-filter helpers.  Pinned by a scripted in-package session over a real address manager: cache states after adds, increments, re-adds, and eviction, the existence and error rows, per-type best submissions, six host conversions incl. resolver paths, and eight local-address resolution scenarios.  Also ported (server.go slice 2a): the serverPeer address relay and ban handler group — the per-peer known-address APBF (10000 items at a 0.001 false positive rate), the server-level addr push that filters known addresses, composes the ported peer push builder, and records the sent subset (dcrd carries an error-and-disconnect branch there, but the peer push never errors at the tag, so it is dead code — pinned by the empty-cache scenario keeping the peer connected), `addBanScore` over the ported dynamic ban score (banning disabled and whitelisted gating, the warn threshold at half the ban threshold, and the threshold-crossing ban), `BanPeer` (the banned-host map entry with the ban duration and the disconnect, with whitelist/disabled gating and the unsplittable-address disconnect-only fallback), `OnGetAddr` (simulation/regression network and outbound-peer fingerprinting gating, the once-per-connection flag, and the address-cache push), and `OnAddr` (the empty-list ban with dcrd's reason, the future-timestamp clamp to five days ago, per-address known marking, and the address manager forward; dcrd samples the connected flag per address against concurrent disconnects — the synchronous port samples it once).  Pinned by a scripted in-package session driving dcrd's real handlers on real serverPeer instances over piped connections (the in-tree peer module replaces the pinned one solely to enable the same self-connection bypass its own tests use; ping/pong round trips order the observations): the getaddr gating rows, the pushed-subset row (dcrd's address cache pick is random, so the row pins the count, cache membership, and the once-flag, and the exact pushed list feeds the replay's cache input), the known-address marks, the empty-list ban, the ban score ladder, and the BanPeer rows.  Also ported (server.go slice 2b): the abuse-control handler group — `OnMemPool` (the decaying 33-point flood score banning a fourth burst request, and the pool's transactions as queued inventory), `enforceNodeCFFlag` for the unsupported version 1 committed filter requests reached from `OnGetCFilter`/`OnGetCFHeaders`/`OnGetCFTypes` (at the negotiated protocol version a single violation scores exactly the ban threshold, which the strictly-greater rule does not cross, so the peer disconnects without a ban entry — pinned; with banning disabled only the disconnect remains), and `OnNotFound` (the block/transaction/mix inventory classification with the invalid-type disconnect, the 20-per-block persistent and 10-per-item transient scores with dcrd's pluralized reason strings, and the sync manager forward, which is ported netsync machinery) with `pickNoun`.  Pinned by scripted sessions over the same piped-peer rig: the four-request mempool ladder, both filter enforcement variants, and the four notfound classifications, each observing the banned-host map and connection state.  Also ported (server.go slice 2c): the version handshake handlers — `OnVersion` (the address manager service-flag update for outbound peers, skipped for inbound peers and on the simulation and regression networks and performed before any rejection; the disconnect for protocol versions below the reject-removal version; the mix-capable outbound maintenance check — evaluated between the two rejections, wanting three mix-capable outbound peers clamped to the outbound target and disconnecting a pre-mix-version outbound peer when the remaining slots are needed, deliberately falling through without returning so the address manager work below still runs and the peer is still marked good, pinned; the disconnect for outbound peers missing the network service flag; and the outbound non-simulation tail: advertising the best local address only when listening and the sync manager reports current — a branch the dump cannot reach without a live sync manager, so it is exercised natively over the slice 1 pieces — requesting more addresses when the address manager wants them, and marking the peer's address good) with the relay-transaction preference taken from the message's disable-relay flag, and `OnVerAck` queueing sendheaders.  Pinned by seven scenarios from a dump driving dcrd's real OnVersion directly on live piped serverPeers (a public-address rig variant, because private-range addresses are unroutable to the address manager, and a zero max-peers config so peer admission safely disconnects each peer after the handler without a live sync manager): both rejections, the mix maintenance fall-through where the address cache still gains the peer, the outbound accept path with its cache and relay observations, the inbound and simnet skips, and the verack response.  Also ported (server.go slice 3): the peer state maps and admission — `peerState` (the inbound/outbound/persistent peer maps, the banned-host map with ban-lift times, the outbound group counts, and the slice 1 submission cache) with its count and connections-per-IP helpers, `handleAddPeer` (the shutdown and unsplittable-address disconnects, the banned-host gate that removes expired bans, the single-IP connection limit with its whitelisted-inbound and loopback bypasses, the max-peers limit with the whitelisted-inbound bypass, inbound map placement where a stored peer-suggested address can only corroborate an existing submission, outbound map placement with the group-key increment, and the discovery tail — skipped for proxies, explicit disablement, external IPs, disabled listening, UPnP, and the simulation networks — that screens the suggested address through the external-candidate check, adds or increments the submission, and re-resolves the local address through the slice 1 machinery), `DonePeer` (tracked-peer removal with the version-known group decrement and connection manager release, and for untracked negotiated peers the address manager connection-time update, skipped on the simulation networks), `disconnectPeer` (dcrd removes the first match in Go's random map order; iteration here is in key order, with the dump avoiding multi-match rows), and `isWhitelisted` over a port of Go's `net.IPNet.Contains` (with the IPv4-mapped network normalization) and `net.ParseIP`.  Pinned by 26 scenarios from a dump running dcrd's real functions on live piped serverPeers and reading the peer state maps, banned hosts, outbound groups, submission cache, and address manager directly: the ban gate and expiry, both limit ladders with their bypasses, corroboration scores, group placement and decrement, the two-peer discovery majority that adds the local address on the second submission, the discovery skips, the untracked-peer timestamp refresh with its simnet skip, six whitelist rows, and the disconnect-peer removals.  Also ported (server.go slice 4): the relay and broadcast decision core — `handleRelayPeerInvMsg` (the not-connected and required-services gates, the block-announcement de-duplication that clears the peer's last announced block when the same block arrives twice and records it otherwise, the headers-versus-inventory preference where a wanted-headers peer receives a headers message built from the announcement's block header and drops the announcement when the data is not a header, the transaction path that gates on the peer's relay-disabled flag and the data being a transaction before tracking it as recently advertised, the mix path that gates on relay-disabled and the negotiated protocol version being at least the mix version, and the immediate-versus-batched queue selection) and the per-peer body of `handleBroadcastMsg` (connected and not in the exclusion set).  Pinned by fifteen scenarios from a dump driving dcrd's real handlers on live piped serverPeers and observing the announced-block field, the recently advertised transaction cache, and the peer-visible inv, headers, and message deliveries (the batched inventory is observed within the 100-to-500ms trickle window): the two gates, the de-duplication, the inventory and headers block-announcement paths with the bad-data drop, the transaction relay with its disabled and bad-data variants, the three mix outcomes, immediate relay, and the broadcast delivery and exclusion.  Also ported (server.go slice 5): the inv and getdata intake gates — `OnGetData` up to the serve queue (the empty-request ban with dcrd's reason; the decaying request ban score that scores an oversized inventory query at ninety-nine hundredths of a full message, so one full query warns without banning while a second crosses the threshold and dcrd bans with the "ban score exceeds threshold" reason; the max-concurrent-request-batch and max-pending-item disconnects; and the enqueue that advances the pending item count) and `OnInv`'s classification (the empty-announcement ban with dcrd's reason, and in blocks-only mode the transaction and mix announcement disconnects with dcrd's nouns, otherwise a forward to the ported sync manager).  Pinned by nine scenarios from a dump driving dcrd's real handlers on live piped serverPeers and reading the banned-host map, the connection state, and the pending getdata item counter directly: the empty ban, the small enqueue, the warn-then-ban score ladder over two full queries, the two capacity disconnects, and the inv empty ban with both blocks-only disconnects (the forward outcome is netsync machinery not reachable from the dump, exercised natively).  Also ported (server.go, chain-backed): `OnGetBlocks` and `OnGetHeaders` response construction — the getblocks inventory build over the ported `LocateBlocks` output (skipping inventory the peer is already known to have, and storing the continue hash when the response fills an entire five-hundred-vector message so the peer's request for the final block triggers the next batch), and the getheaders decision over the ported `LocateHeaders` and `ChainWork` outputs (an empty headers message when the local best known tip's cumulative work — compared against the minimum known work by the ported uint256 ordering — is too little, otherwise the located headers, with a chain-work lookup error skipping the empty-response gate).  The chain query walks themselves are the ported blockchain engine's, pinned separately by its own vectors; these pin the server-specific wrapping.  Pinned by seven scenarios from a dump that built a real regnet chain from dcrd's own fullblocktests generator (twenty-five accepted blocks processed through `blockchain.ProcessBlock`) and drove dcrd's real handlers over a live piped serverPeer, freezing the located hashes/headers as row inputs and observing the peer-visible inv and headers deliveries: the genesis-locator, stop-hash, at-tip (empty), and known-inventory-filtered getblocks rows, and the normal, at-tip, and low-work getheaders rows; the continue-hash-on-full-message branch is unreachable below five hundred blocks and is pinned directly (the block hashes differ per generation because the generator stamps wall-clock timestamps, so each row is self-contained).  Also ported (server.go, chain-backed): `handleServeGetData` — the getdata serve queue as a decision core over the per-inventory fetch resolutions (a transaction from the recently advertised cache or mempool, a block from the chain, a mix message from the mix pool): each resolved data message is queued in request order, the misses are accumulated into a single consolidated notfound message sent last, a served block that was the advertised continuation triggers a best-tip inventory message afterward and clears the stored continuation hash, an unknown inventory type is skipped entirely (neither served, recorded as not found, nor counted against the pending decrements), and served and not-found items each decrement the pending item counter; dcrd's send semaphore and pipelining are concurrency machinery and are not reproduced, and the item fetches are the caller's seams.  Pinned by seven scenarios from a dump that drove dcrd's real handleServeGetData over a real regnet chain (built from the fullblocktests generator) with empty mempool and mix pools, recording the ordered sequence of messages the remote received: the multi-block serve, the block/transaction/mix not-found cases, the unknown-type skip, a mixed batch interleaving served blocks with a miss, and the continuation-hash block that appends the best-tip inventory and clears the hash.  Also ported (server.go, chain/mempool-backed): `OnGetInitState` — the init state response assembly as a decision core over the requested types and the `SortParentsByVotes`/`VoteHashesForBlock`/`TSpendHashes` seam outputs: the once-per-connection guard, the empty response before stake validation height, fetching the head blocks (capped at eight) when either they or their votes are requested, extending the votes per capped block in order, passing through the treasury spends, clearing the head blocks when only votes were requested, and dropping the response as a build error when a wire list limit is exceeded.  Pinned by seven scenarios from a dump driving dcrd's real OnGetInitState over real regnet chains built from the fullblocktests generator (one past the stake validation height, one below it) with an empty mempool: the below-stake blank push, the once-flag second-request suppression, and the type combinations — the empty mempool leaves the seams empty so the dump pins the once-flag, the blank gate, and the past-stake empty push, while the block cap, the vote extension, the clear-when-only-votes rule, and the wire limits are pinned directly with crafted facts (a voting mempool is not cheaply seedable in the dump).  Also started: the `dcroxide` daemon binary — the runtime front-end of `dcrd.go`'s `dcrdMain`: it builds the configuration environment from the real operating system (the application data directory with Go's GOOS names, the localhost resolver, and the system random source; interface-name and cross-user home resolution are not yet wired), parses the command line through the ported configuration pipeline, handles the help, version, and `--debuglevel=show` exits with dcrd's exit codes (the show exit is now surfaced as a distinguished result mirroring dcrd's `os.Exit(0)` right before the debug level parse), prints the startup banner, opens (or creates) the block database at `<datadir>/blocks_<dbtype>` and initializes the chain state through the ported chain engine (a fresh database creates the genesis chain state, an existing one is reopened), announces the loaded best block height and hash, creates the address manager and loads the persisted `peers.json`, announces the loaded address count and the configured peer-to-peer listeners and seeding, binds the peer-to-peer listeners and serves inbound peers through the threaded listener runtime (building the peer template from the network, `SFNodeNetwork`, the user agent, and the peer idle and ping timeouts, then starting the runtime with the inbound-peer handler), and idles on a SIGINT/SIGTERM shutdown listener until interrupted, at which point it disconnects the live peers and stops accepting new connections.  Served peers' messages now flow through the server-handler dispatch: a per-peer handler (the message-handling slice of dcrd's `serverPeer`) over a shared `ServerContext` (the chain behind a mutex plus the network's minimum known chain work and the banning configuration) answers `getheaders` through the ported low-work gate and `LocateHeaders`, `getblocks` through `LocateBlocks` with the known-inventory skip and the continue-hash bookkeeping, and `getdata` through dcrd's intake gates (empty-request ban, the decaying oversized-request ban score with the whitelist exemption, the request limits) and the serve queue — blocks fetched from the chain, transaction/mix requests answered in the consolidated notfound exactly like empty pools, unknown inventory types skipped, and the continuation triggering the best-tip inventory (the ban outcomes currently drop the connection; the ban-list bookkeeping refusing reconnects arrives with the peer-state wiring; dcrd's asynchronous serve queue with its semaphore is served inline on the input thread, so batches never overlap by construction).  The stake ticket treap's structural sharing moved from `Rc` to `Arc` so the chain crosses into the peer threads (result-invariant; the dcrd treap battery pins the shape).  The addr exchange is also dispatched: getaddr answers an inbound peer once per connection with the randomized address-cache subset through the ported pushAddrMsg (known-address filtering and tracking), and addr intake clamps far-future timestamps, tracks the addresses as known to the peer, and feeds the shared address manager, with an empty list dropping the connection.  The committed-filter and init-state handlers are also dispatched: getcfilterv2 serves a block's version 2 filter with its header commitment inclusion proof and getcfsv2 the batched ancestry range (both silently ignoring unknown blocks and missing filters exactly like dcrd), and getinitstate answers once per connection — the empty message before stake validation, and the filled response past it (whose eligible head blocks, votes, and treasury spends await the mempool and tip-generation wiring; the daemon cannot sync past stake validation before the sync manager lands).  Inventory announcements pass the ported intake gate (an empty announcement drops the connection, and blocks-only mode disconnects peers announcing transactions or mix messages), with the passing announcements' forward awaiting the sync-manager driver.  The netsync seams are in place: a chain adapter hands the sync manager the shared chain (injecting the system clock where dcrd reads its median-time source, identical before any samples), the not-yet-wired mempool and mixpool stand in as empty pools that reject everything, and the daemon constructs the manager with dcrd's config surface (the params-derived minimum work and target block time, the recently-confirmed APBF at dcrd's 23000/1e-6 sizing, and targetOutbound capped by --maxpeers) — the chain gained dcrd's exported BestHeader/HaveHeader/HaveBlock/IsCurrent/MaybeUpdateIsCurrent wrappers for it.  The sync manager is now driven: each handshaken peer is registered with it through the serve lifecycle hooks (dcrd `AddPeer`/`DonePeer` signalling `OnPeerConnected`/`OnPeerDisconnected`), a peer-id registry resolves the manager's actions to any live peer's outbound queue or socket (queue-message and disconnect actions; the stall-timer actions await the timer piece), and the headers/block/transaction intake plus the passing inventory announcements flow through the manager's decision cores — so a data-serving peer connecting to a stale chain receives the daemon's getheaders immediately after the handshake, the beginning of the initial header sync.  The header-sync stall watchdog runs as its own timer thread: the manager's reset/stop actions arm and disarm it, and when the armed timeout elapses the manager's stall handler disconnects the unresponsive sync peer (dcrd's stallHandler timer case, at dcrd's timeout with the duration injected for tests).  The connection manager drives outbound dials: an event-loop driver over the ported decision-core `ConnManager` opens the permanent `--connect` connections, carries each dialed socket into the shared serve path (so a dialed peer runs the same `serverPeer` dispatch and registers with the sync manager, the direction that actually syncs the daemon), and keeps them up with the manager's retry backoff — the daemon now seeks out peers rather than only accepting them.  The address-manager automatic dialing now feeds the same driver: the ported `newAddressFunc` draws candidates from the address manager, skipping a group the daemon already has an outbound connection to (tracked as dcrd's `outboundGroups`), recently attempted addresses, and non-default ports on early tries, and the manager fills its target-outbound slots from that source when no `--connect` peers are set; a dialed peer's version exchange marks the address good, requests more addresses when the manager is low, and advertises the best local address to a current, listening node (dcrd `OnVersion`'s outbound branch).  The HTTPS seeders bootstrap the address manager: each configured seeder is queried on its own thread through a TLS transport (`ureq` over rustls — the workspace's first TLS dependency, chosen with the future TLS RPC server in mind) with dcrd's one-minute timeout, the ported response handling lands the discovered addresses with the seeder's resolved IP as their source, and a fully failed round retries on dcrd's one-to-ten-second backoff while the manager still needs addresses.  The RPC server now answers over HTTP: a per-connection threaded listener parses requests with dcrd's authenticated read limit, authenticates through the ported `checkAuth` (dcrd's 401 with its realm on failure), and runs bodies through the ported `jsonRPCRead` pipeline over a chain adapter (a growing slice of the handler seam trait — the chain adapter now delegates the block/header/height/median-time/live-ticket/ticket-pool queries straight to the chain, a connection-manager adapter answers the connected count over the live-peer registry, and a sync-manager adapter answers the sync height and peer id, so getbestblockhash/getblockcount/getblockhash/getblock/getbestblock/getconnectioncount and their kin answer over the running daemon; getinfo, getblockchaininfo, and getnettotals answer too — the chain gained dcrd's exported MaxBlockSize (the max-block-size vote over the branch view), the threshold-state tuple converts onto the RPC agenda states (the winning choice dropped exactly as dcrd's status mapping does), a zero-offset time source stands in dcrd-faithfully until the median-time port collects samples, the system clock backs dcrd's rpcClock, and every peer transport feeds a server-wide received/sent byte-totals pair from the handshake onward (dcrd's OnRead/OnWrite accounting; the port counts a message's bytes on successful decode, so a rejected oversized header contributes nothing where dcrd would count its 24 header bytes — noted divergence); a handler touching a still-unwired daemon seam (the mempool, mining, and indexer subsystems) answers an internal error and the server survives); the RPC machinery's boxed subsystem seams gained `Send` bounds so the server crosses the connection threads.  dcrd's default TLS mode is served: the daemon loads or generates the `rpc.cert`/`rpc.key` pair exactly as `genCertPair` does (the ported certgen's P-256 pair with dcrd's ten-year validity and autogenerated organization, the key written owner-only) and wraps each RPC connection in a rustls server session — noting that dcrd's autogenerated certificate is a self-signed CA served directly as the end-entity certificate, which Go clients accept but webpki-based clients must pin exactly as Decred tooling pins rpc.cert (the end-to-end check does the same).  The websocket endpoint is served too: a `GET /ws` request is authenticated (allowing an unauthenticated upgrade that must authenticate in-band), validated and answered with the RFC 6455 handshake (the accept key is the SHA-1-and-base64 of the client key and the protocol GUID), and then each JSON-RPC request arrives as a masked text frame through a from-scratch RFC 6455 server frame transport (fragment reassembly, ping/pong, the cumulative read limit, protocol-error close frames) and runs dcrd's `inHandler` gate — an unauthenticated client must `authenticate` first, limited users are refused non-limited methods, null-id notifications draw no reply — before dispatching through the ported websocket service handler; the subscription commands are recorded by a notification manager that now also DELIVERS: a delivery thread (dcrd's `notificationHandler` goroutine in threaded form) receives chain and mempool events over a channel, runs the ported per-event builders against the subscribed clients (block connected/disconnected with the per-client filtered transaction list, work, tspend, reorganization, winning tickets, new tickets, mempool acceptance with the relevant-transaction filter pass over every client, and mix messages), and queues the marshalled JSON on each client's outbound queue, which the serving loop drains whenever the connection goes idle (the read timeout doubles as the notification poll, replacing the 30-second connection timeout that would otherwise kill idle websocket clients dcrd keeps alive indefinitely); a disconnecting client is removed from the registry and every subscription map except mix messages, exactly the map dcrd's unregister-client case skips.  The daemon does not yet feed the delivery channel from its own chain events — the blockchain notification callbacks (dcrd `blockchain.Notification`) arrive with a later piece.  The UTXO set now backs the RPC seams too: `gettxout` resolves entries through `Chain::fetch_utxo_entry` over the loaded set (falling through an empty-mempool `FetchTransaction` stub that answers dcrd's transaction-is-not-in-the-pool error, discarded like dcrd's handler does), `gettxoutsetinfo` runs a ported `FetchStats` (a forced cache flush that also writes the utxo set state, then a fold over the set in serialized-key byte order — including dcrd's VLQ quirk where output index 16512 sorts before 16511 — producing the counts, distinct-transaction tally, serialized size, total atoms, and the merkle root of the BLAKE-256 entry hashes), and `createrawssrtx`'s ticket lookup decodes the entry's stored minimal outputs through the ported `deserializeToMinimalOutputs`; the treasury and auto-revocations agenda-active queries answer over the chain's deployment state with dcrd's zero-hash special case.  The mempool itself arrives with a later piece, as does the rotating file-logging backend (startup output currently goes to the standard streams), and the network-interface and cross-user home resolution used by a small part of the configuration pipeline are not yet wired.  Pinned by command line integration checks over the built binary (the version, help, debug-level show, and unknown-flag exits, plus a fresh-database `--nolisten` startup that opens the block database and loads the genesis chain state at height zero, and a `--listen=127.0.0.1:0` startup that announces it is serving peers on the bound ephemeral port), and by end-to-end dispatch checks over a genesis chain served through the full runtime (a negotiated peer's getheaders answered with the empty low-work headers message, a getdata served the genesis block from the chain with misses consolidated into notfound, an empty getdata request dropping the connection through the intake gate, the genesis committed filter served with its inclusion proof singly and batched, and the pre-stake-validation empty init state sent exactly once per connection).  Also started (the threaded server runtime, translating `server.go`'s `Run`/`peerHandler` goroutines to OS threads and channels rather than an async runtime, per the project's concurrency decision): the listener backbone (`ListenerRuntime`) — it binds each parsed listener spec (expanding the wildcard host to the family-appropriate any-address for the `tcp4`/`tcp6` split), accepts inbound connections on a dedicated non-blocking thread per listener that hands each accepted connection to a supplied handler, and coordinates a graceful shutdown by signalling those accept threads and joining them; the connection manager's outbound dialing and seeding, the peer version handshake, the per-peer input and output loops, the sync manager, and the RPC server plug into this same shutdown coordination and, together with wiring the runtime into the binary's `run`, arrive with later pieces.  Pinned by integration checks that bind ephemeral listeners, connect to the assigned ports, and confirm both single- and multi-listener inbound acceptance and a clean joined shutdown.  Also started (the connection framing): `WireTransport` — the concrete `MsgTransport` the ported peer module's version handshake and per-peer message loops run over, framing whole wire messages over any byte stream (a TCP connection in the daemon, an in-memory stream in tests) with the ported wire codec — reading the fixed 24-byte header first and rejecting a header-declared payload length above the global cap before any payload allocation (so a hostile peer cannot force a huge read), and tallying the wire bytes moved in each direction for the peer loop's byte accounting; the idle read deadline dcrd sets before each read is applied by the peer loop on the underlying stream.  Pinned by unit checks (the framed bytes match the wire codec, an empty-payload and a payload-carrying message round-trip through an in-memory stream, and an oversized header length is rejected without reading a payload) and an integration check that round-trips messages both directions over a real loopback TCP connection.  Also started (the peer environment and handshake): `NodePeerEnv` — the daemon's concrete `PeerEnv`, supplying the ported peer module's injected clock (the wall clock in unix nanoseconds), randomness (the system random source for the version nonce), and address shuffle (a Fisher-Yates permutation over that source, standing in for Go's `rand.ShuffleSlice`) — together with the conversion from an accepted socket address into the wire network address a peer is associated with; with these a `Peer` runs the ported version negotiation straight over the `WireTransport`.  Pinned by unit checks of the environment (the clock is a recent positive time, the random source varies, the shuffle preserves the address multiset, and the socket conversion keeps the raw IP bytes) and an integration check where an inbound and an outbound peer complete the full version exchange over a real loopback TCP connection and each learns the other's advertised protocol version, services, and user agent.  Also started (the per-peer input pump, `inHandler`): `classify_incoming` — the message-type dispatch as a decision core over the ported peer handlers (a second version or a second verack disconnects with dcrd's reason, a first verack marks the peer, a ping yields the matching pong reply, a pong records the ping round-trip, a sendheaders sets the wants-headers preference, and every other message is forwarded to the server handlers) with `send_verack` (dcrd `start`'s post-negotiation `QueueMessage(verack)`) and `run_peer_input`, the read loop that queues each immediate reply on the outbound queue and forwards each message until a read error (a closed connection or an idle read timeout) or a protocol disconnect.  dcrd's stall detector arrives later.  The read and write halves share the peer as a `&Mutex<Peer>` — the blocking read is taken without the lock held so the ping timer and the server make progress, and every write (the input pump's protocol replies included) goes through the outbound queue so all writes stay on the single output loop.  The keepalive is `run_ping_timer`, which every interval draws a fresh nonce, records it on the peer (so the answering pong can be timed), and queues a ping, until a shutdown signal or the queue closes (dcrd `pingHandler`).  Pinned by unit checks of the dispatch (ping-to-pong, the first-then-duplicate verack, the second-version disconnect, the sendheaders flag, and the pong clearing the outstanding ping); an integration check where the inbound peer, after the handshake, queues its verack, splits the socket, runs the output loop on its own thread, and runs the input loop over the shared peer — marking the remote's verack, answering a ping with the matching pong through the queue, and forwarding both messages in order — over a real loopback TCP connection, ending when the remote closes; and a ping-timer check that a shared peer's timer thread queues and records a keepalive ping and stops on the shutdown signal.  Also started (the per-peer output handler, `outHandler`): `OutboundQueue` — a cloneable queue handle (dcrd `QueueMessage`) the server, the input pump's replies, and the ping timer send through — and `run_peer_output`, the writer loop that drains the queue and writes each message to the connection until the queue is closed (a local shutdown) or a write fails, so all writes to a connection are serialized on one thread.  dcrd's separate inventory trickle queue (`QueueInventory`) and the send semaphore are later refinements; this is the plain message queue.  Pinned by an integration check that queues messages, drains them over a real loopback TCP connection, and confirms they arrive in the order queued and the loop shuts down cleanly when the queue closes, plus a check that queueing to a closed queue fails.  Also started (the connection assembly): `run_peer_connection` — dcrd `peer.go`'s `start` plus the per-peer goroutine set as OS threads: it sets the read deadline to the idle timeout, splits the socket into read and write halves, runs the version handshake (inbound or outbound per the peer) over the read half before starting the loops — framing the handshake at the local maximum protocol version and then lowering both transports to the negotiated version, as dcrd re-reads the peer's protocol version per message — shares the negotiated peer as an `Arc<Mutex<Peer>>`, queues the verack, runs the output loop and the ping timer on their own threads, drives the input loop on the calling thread, and on disconnect shuts the socket down (so a write blocked against a peer that stopped reading unblocks, as dcrd's `Disconnect` closes the conn), signals the ping timer, closes the outbound queue, and joins both threads before returning why the connection stopped.  Pinned by an integration check where the server side runs the whole connection — accept, associate, handshake, and serve — while a remote peer negotiates, exchanges verack, and pings, receiving the pong, then closes: the connection forwards every message in order and stops with a read error, with the ping interval and idle timeout set long so neither fires.  Also started (serving inbound peers): `inbound_peer_handler` — the listener runtime's accept handler (dcrd `server.inboundPeerConnected`): each accepted connection is handed to its own thread, which builds a fresh inbound peer from a `PeerTemplate` (network, advertised services, user agent, and the idle and ping timeouts), associates it with the remote address, and runs the whole connection; the server-handler dispatch (relay, inv, getdata) is a no-op until the peer-handler piece, so a served peer completes the handshake and is kept alive with ping and pong.  Each live connection is tracked in a `ConnectedPeers` registry (a socket handle per peer) so a shutdown can disconnect them all — shutting down each socket unblocks the peer's read loop so it winds down (the connected-peer half of dcrd's `peerState`); a drop guard deregisters a peer even when its serving thread unwinds from a panic, the registry's locks recover from poisoning so a crashed peer thread cannot break the shutdown sweep, and a transient `accept` error waits out a poll interval instead of killing the listener (dcrd logs and keeps accepting).  Pinned by an integration check that starts a listener runtime with the serving handler, connects as an outbound peer, negotiates (learning the server's user agent), and confirms the server answers a ping with a pong before a clean shutdown; and a check that a served peer registers as live, then deregisters once `disconnect_all` tears its connection down.  Not yet ported: the rest of server.go (`OnGetMiningState` and the mix handlers, the CF-v2 filter server handlers' `QueueMessage` plumbing, the tx/block/mix intake forwarding, the rebroadcast inventory prune, and the block-database/server lifecycle — sliced in the progress notes), the tool commands (`gencerts`/`addblock`/`promptsecret`), and the Windows service wrapper |
