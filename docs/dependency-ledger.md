# Consensus- and security-critical dependency ledger

[SECURITY.md](../SECURITY.md) states the gap this file starts to close:
*"Nobody has read the dependencies."* `cargo-deny` runs on every push and
gates the RustSec advisory database, a licence allow-list, yanked crates, duplicate major versions and
unknown sources — an automated check against problems someone else already
found, not a review.

This ledger does not claim a review has happened. It records **a decision
per dependency**, so the trust status of the load-bearing subset is explicit
rather than implied, and so a version bump of one of them is a decision
rather than a silent `Cargo.lock` change. The precedent is
[ADR-0002](adr/0002-vendor-blake256-from-dcr-rs.md): BLAKE-256 was vendored
into the tree precisely because it is consensus-critical.

Cuprate reached the same problem from the other side — pinning crypto and
storage crates to exact git revisions, maintaining frozen forks, and
eventually co-maintaining its curve library. Most of those rungs do not fit
a single-maintainer pre-alpha, and the sizes below are why: vendoring
`ring` would mean adopting 151k lines of C and assembly.

## Decision vocabulary

- **vendored** — the code is in this tree, pinned and attributed.
- **reviewed@V** — a human has read the parts this node depends on, at
  version V. Bumping past V requires a fresh look.
- **accepted** — used as-is on stated grounds, without a read. This is an
  honest status, not a placeholder: most entries below are here.
- **declined** — considered and not adopted.

The scope is the ~12 crates whose behaviour is observable in consensus, key
handling, or the network surface, out of roughly 210 packages in the lock file,
plus `windows-sys`, the one crate the workspace's own `unsafe` code calls.
Everything else is `accepted` by default and not listed.

## The set

| crate | version | role | decision | notes |
|---|---|---|---|---|
| `secp256k1` | 0.33.1 | ECDSA (signature type 0) | accepted | Bindings to the audited libsecp256k1 C library, per [ADR-0006](adr/0006-secp256k1-backend.md). dcrd's acceptance rules are not in this crate — DER parsing, low-S, error identities are all implemented in `dcroxide-dcrec` in front of it and differential-tested against dcrd. Bumped from 0.29.1, still accepted. 0.33.0 removed the context object from the API (its `CHANGELOG.md`, #844), so `dcroxide-dcrec` now calls the context-free functions (`ecdsa::verify`, `ecdsa::sign`, `RecoverableSignature::recover_ecdsa`, `SecretKey::public_key`), which run against a context the crate keeps internally: with `std`, one per thread, created on first use (`src/context/internal_std.rs:12-14`); without it, one on the stack for each call (`src/context/internal_nostd.rs:109-124`). `global-context` is no longer enabled, and `alloc` is no longer requested directly; secp256k1's own `std` still turns it on in every `std` build. |
| `secp256k1-sys` | 0.14.1 | vendored libsecp256k1 | accepted | ~67k lines of vendored C. Declining to vendor further: this is the most widely deployed secp256k1 implementation in the ecosystem and re-vendoring it here would fork that scrutiny, not add to it. It is also the reason the no_std check gates only the Rust side (see the CI job comment). Bumped from 0.10.1, which vendored libsecp256k1 v0.4.1, still accepted: 0.14.1 vendors v0.8.0 (`CHANGELOG.md` under 0.14.0; 0.14.1 is metadata-only), and the vendored revision is recorded in `depend/secp256k1-HEAD-revision.txt`. |
| `k256` | 0.14.0 | EC-Schnorr-DCRv0 group ops | accepted | Pure Rust. Used for raw scalar/point arithmetic no packaged signing API exposes. Bumped from 0.13.4, still accepted, and its `precomputed-tables` feature is no longer enabled. Schnorr signing multiplies by the generator as `ProjectivePoint::GENERATOR * k`, which goes through `lincomb` (`src/arithmetic/mul.rs:236-238`, `:249-256`), and verification computes `s*G + e*Q` in one `MulByGeneratorVartime::mul_by_generator_and_mul_add_vartime` call, a GLV/wNAF pass over the generator and the key (`:303-310`). Neither reads the basepoint table; only `mul_by_generator` and `mul_by_generator_vartime` do (`:180-203`, `:207-232`), and the Schnorr code calls neither, so the table bought nothing. `k256`'s `std` is off as well: at 0.14.0 it enables `getrandom` (`Cargo.toml:129-133`), and `dcroxide-dcrec` draws no randomness. |
| `curve25519-dalek` | 5.0.0 | Ed25519 (signature type 1) | accepted | ~35k lines. dcrd's exact (2017-agl) acceptance behaviour lives in `dcroxide-dcrec::edwards` on top of it, not in it. Bumped from 4.1.3, still accepted, on an upstream source diff of the paths `edwards` calls, not a review. Those paths give the same results in both versions. `CompressedEdwardsY::decompress`, `sqrt_ratio_i`, `Scalar::from_bytes_mod_order`, `from_bytes_mod_order_wide`, `from_canonical_bytes` and `vartime_double_scalar_mul_basepoint` are byte-identical, and `FieldElement51::from_bytes` still masks bit 255 of a non-canonical y without reducing it, now as a `const fn`. `EdwardsPoint::compress` now goes through `to_affine()` with the same arithmetic (`src/edwards.rs:564-569`, `src/edwards/affine.rs:71-75`). `Scalar52::sub` was rewritten to add ℓ back with `subtle`'s constant-time `conditional_select` in place of a `black_box`ed mask, with the same arithmetic (`src/backend/serial/u64/scalar.rs:177-204`). AVX-512 is now compiled in only when the build's target features include `avx512ifma` and `avx512vl` (`build.rs:100-115`); 4.1.3 compiled it into any nightly build and chose it by runtime CPU detection (`src/backend/mod.rs:56-63`). |
| `blake3` | 1.8.7 (reviewed at 1.8.5) | DCP0011 v2 PoW hash | **reviewed@1.8.5, `pure`** | One call site: `dcroxide-wire/src/blockheader.rs`, hashing a 180-byte header. The crate ships ~34k lines of C and SIMD assembly covering every platform it supports; a default build on x86_64 Linux runs a C toolchain over four of those files (SSE2/SSE4.1/AVX2/AVX512, ~11k lines, one object each) to accelerate long inputs. At 180 bytes that acceleration is worthless, so the `pure` feature is enabled: no C toolchain, no assembly, identical output (verified against the official BLAKE3 KATs for the empty string, `abc`, and a 180-byte input). The lock has since moved to 1.8.7, which is 613 lines of source diff across `lib.rs`, `platform.rs`, `hazmat.rs` and `io.rs` — past the reviewed marker, so by this file's own rule it wants a fresh look. What that bump cannot do silently any more is change the hash: the KAT check the note describes was done by hand and kept nowhere, and is now held by `blake3_still_answers_the_known_vectors` (`dcroxide-wire/src/blockheader.rs`), since `pow_hash_v2` is consensus. |
| `redb` | 4.3.0 | chain metadata store | accepted, [ADR-0004](adr/0004-storage-backend.md) | ~54k lines of Rust. Chosen for crash-safety without a C toolchain; its commit shape is the tracked cost. Read closely in one respect — the free-page and transaction-tracker behaviour analysed in ADR-0004's 2026-08-07 addendum — but not reviewed as a whole. Bumped from 2.6.3 on 2026-08-13 (`7d2fe28`, and ADR-0004's addendum of that date): two majors, and the on-disk format changed with them. redb 4 reads only file format 3 and returns `UpgradeRequired` for a 2.x file, so a data directory written before that date is refused with a typed error rather than misread, and has to be re-synced. The lock no longer carries 2.6.3 in any form: the pinned `redb2 = { package = "redb", version = "=2.6.3" }` dev-dependency that wrote a genuine v2 store for that refusal test was dropped on 2026-09-18, with the test, because dcroxide has never been released and no store in the old format exists anywhere to refuse. The `UpgradeRequired` mapping stays and stops naming a version, so it covers whatever the next format change is. Bumped again from 4.2.0 to 4.3.0, still accepted, with no file-format bump: format 3 is still the newest version, the one each transaction header is written with, and the only one read (`src/tree_store/page_store/page_manager.rs:71`, `header.rs:490-512`). 4.3.0 stores shortened separators in branch pages for `&[u8]` keys, the metadata table's key type (`CHANGELOG.md:70-73`, `src/tree_store/btree_base.rs:31-42`). File locking changed with it: byte ranges are now locked with open-file-description (OFD) `fcntl` locks on Linux and Apple targets (`src/tree_store/page_store/file_backend/range_lock.rs:377-415`) and with `LockFileEx` region locks on Windows (`:90-95`), and on Linux a writer also takes the whole-file `flock` that earlier 4.x releases took, for best-effort compatibility with them (`file_backend/optimized.rs:87-104`). |
| `ring` | 0.17.14 | TLS crypto (via rustls) | accepted | ~151k lines of C and assembly. Reachable from two directions: the RPC/websocket server, which requires credentials and is off under `--norpc`, and the HTTPS seeder, which runs at startup by default and is off only under `--noseeders` (`crates/dcroxide-node/src/seeding.rs` drives it through `ureq`, whose rustls provider is also `ring`). Vendoring is not viable at this size for this project. |
| `rustls` | 0.23.45 | RPC/websocket TLS | accepted | Same reachability as `ring`. Bumped from 0.23.43, still accepted, for RUSTSEC-2026-0285 (TLS 1.3 handshake messages accepted across encryption-level boundaries), which affects 0.23.13 through 0.23.44 and failed `cargo-deny`'s advisories check at 0.23.43; `dcroxide-node` now requires `0.23.45` as its floor. The HTTPS seeder's two TLS clients (the proxied `ProxySeederTransport` and `ureq`) trust only the compiled-in `webpki-roots` snapshot, not the platform store dcrd's Go client uses; see PARITY.md's Known remaining gaps. |
| `getrandom` | 0.2.17 / 0.3.4 / 0.4.3 | entropy | accepted | Three majors coexist, and two are in the shipped binary. 0.4.3 is what dcroxide calls directly (nonces, the orphan-eviction draw, secret files) from `dcroxide-crypto`, `dcroxide-certgen` and `dcroxide-node`, and what the `elliptic-curve` 0.14 stack under `p256`/`p384`/`p521` reaches through `crypto-bigint` 0.7.5 and `crypto-common` 0.2.2. 0.2.17 arrives only through `ring`, which is `rustls`' crypto provider for both the RPC server and `ureq` — so it is on the production TLS path from two directions, not one. 0.3.4 is dev-only, through `proptest` → `rand` 0.9.5 → `rand_core` 0.9.5. `dcroxide-dcrec` on its own pulls in no `getrandom`. Collapsing the two production majors now waits only on `ring`, and 0.17.14 is its newest release. Bumped from 0.3.4 as the direct major, still accepted. On Windows 0.4.3 returns `Error::UNEXPECTED` when `ProcessPrng` fails (`src/backends/windows.rs:68-72`), where 0.3.4 only `debug_assert!`ed the result and returned `Ok` with the buffer unwritten (`src/backends/windows.rs:59-60`). |
| `p256`, `p384`, `p521` | 0.14.0 | RPC certificate generation; `p521` also signs RPC TLS handshakes | accepted | Pure Rust, which is why they sit here rather than under a C provider. `p384` is still `#![forbid(unsafe_code)]` (`src/lib.rs:7`); `p256` and `p521` no longer carry that attribute at 0.14.0, though neither has any `unsafe` in its `src/`. `dcroxide-certgen` uses `p256` and `p521` to generate and self-sign the RPC certificate for `--tlscurve`, and all three in the `gencerts` tool. Only `p521`'s field arithmetic is fiat-crypto output (`src/arithmetic/field.rs:3`); `p384` 0.14.0 defaults to a `crypto-bigint` backend and uses fiat-crypto only under `--cfg p384_backend="fiat"` (`src/lib.rs:11-32`), and `p256` contains no fiat-crypto code. All three ship the same warning that the curve arithmetic "has never been independently audited" and that constant-time generated assembly has not been assessed. For `p256` and `p384` that exposure is one signature over a message the port chose. `p521` also signs every RPC TLS handshake when the RPC key is on P-521 (`rpcrun::P521SigningKey`), since `ring` cannot, so for that key the warning reaches the handshake path. Bumped from 0.13.2, 0.13.1 and 0.13.3, still accepted. `p521` now signs through `ecdsa` 0.17's `SigningKey` (`src/ecdsa.rs:62`), whose `Signer` derives the nonce deterministically with RFC 6979 (`ecdsa-0.17.0/src/signing.rs:166-201`); hedged signing, which mixes RNG output into that derivation, is available through `RandomizedSigner` (`:220-249`) and is what the handshake signer uses. That RFC 6979 core reproduces RFC 6979's A.2.7 vector byte for byte, as OpenSSL does, despite an upstream TODO calling the vector test failing; [PARITY.md](../PARITY.md) records the check, the side-by-side run against dcrd, and the TLS1.2 difference that serving P-521 leaves. |
| `sha2`, `hmac` | 0.11.0, 0.13.0 | RFC6979 nonces; `sha2` also the RPC certificate's SubjectKeyIdentifier | accepted | RustCrypto, widely used; dcrd's exact RFC6979 variant is ported in `dcroxide-dcrec`, not taken from these. `dcroxide-certgen` depends on `sha2` directly as well: the SubjectKeyIdentifier it writes is Go 1.25's leftmost 160 bits of SHA-256 over the public key (`subject_key_id`, `crates/dcroxide-certgen/src/x509.rs`), which replaced the SHA-1 form, and non-ASCII host names go through a port of Go's `idna.ToASCII` (`crates/dcroxide-certgen/src/punycode.rs`) rather than the `idna` crate's UTS-46. certgen therefore no longer depends on `sha1` or `idna`, and the lock dropped `idna` with its ICU stack (`idna_adapter`, the seven `icu_*` crates, `zerovec`, `yoke`, `zerofrom`, `zerotrie`, `litemap`, `tinystr`, `smallvec` and their helpers), 25 packages in all. `sha1` 0.11.0 stays, for `OP_SHA1` in `dcroxide-txscript` and the websocket handshake in `dcroxide-node` (`wsframe.rs`). Bumped from 0.10.9 and 0.12.1, still accepted; the only non-test change was importing `hmac::KeyInit` for `new_from_slice`. From 0.11 `sha2` also selects hardware backends at runtime on aarch64, where 0.10 ran portable code, so `sha512_still_answers_the_known_vectors` (`dcroxide-dcrec/src/edwards.rs`) and `sha1_and_sha256_still_answer_the_known_vectors` (`dcroxide-txscript/src/opcodes.rs`) now hold FIPS 180's digests on whichever backend runs. |
| `siphasher` | 1.0.3 | GCS filter hashing | accepted | Consensus-observable through `dcroxide-gcs`; the filter construction itself is ported and differential-tested. |
| `num-bigint` | 0.5.1 | difficulty math | accepted | Used by `dcroxide-standalone`, which re-exports `BigInt` and `Sign`; by `dcroxide-blockchain` through that re-export, including chain work in `calc_work_uint256`; by `dcroxide-rpc` directly; and by `dcroxide-mining` through the re-export. Every crate that depends on `dcroxide-chaincfg` links it too, because chaincfg depends on `dcroxide-standalone` for the `SubsidyParams` impl on `Params`; chaincfg itself calls none of it. The uint256 path that matters most is `dcroxide-uint256`, which is a port, not a dependency. Bumped from 0.4.8, still accepted. The move changes no arithmetic source: between the two releases `src/` differs only in `lib.rs`'s docs and re-exports and in `bigrand`, where the `rand` feature was split by `rand` version. The `0.5.1` floor excludes 0.5.0, which is yanked. |
| `windows-sys` | 0.61.2 | Windows FFI declarations for `dcroxide-winsvc` | accepted | Listed because it is the one crate the workspace's own `unsafe` code calls into, not for consensus or key material. Microsoft's raw bindings, generated from the Windows metadata, almost wholly `extern` declarations, types and constants. `dcroxide-winsvc` has taken it directly since 2026-09-25, at the version the lockfile already carried through `windows-service`, `ctrlc`, `rustix`, `socket2`, `tempfile` and `errno`, so the lock gained no package. It enables three features (`Win32_Foundation`, `Win32_System_Console`, `Win32_System_Threading`) and calls three functions: `SetConsoleCtrlHandler` for the console close, logoff and shutdown handler, and `GetCurrentProcess` with `DuplicateHandle` to adopt an inherited `--piperx`/`--pipetx` handle. Each call sits in an `#[allow(unsafe_code)]` block with a `SAFETY` comment ([ADR-0008](adr/0008-clippy-lint-policy.md)'s 2026-09-25 addendum). The functions bind through `windows-link` 0.2.1's `raw-dylib` imports of `kernel32.dll` (`src/lib.rs`), so no import library is involved. The three declarations were compared with the Win32 signatures when the dependency was taken; the crate as a whole has not been read. |

## What is deliberately not here

`cargo-vet` is not wired up. It is the natural mechanical backing for this
ledger — audits committed to the repo, so bumping a critical crate requires
a recorded decision — and it should be adopted when there is more than one
maintainer to exchange audits with, or when the project imports another
organisation's audit set. For a single maintainer it would currently
restate this table in a format nobody else reads. Recorded as a decision
rather than an oversight.

Vendoring beyond ADR-0002 is declined for every crate above, on size:
`ring` is ~151k lines, `secp256k1-sys` ~67k, `curve25519-dalek` ~35k,
`redb` ~54k. Each figure is `wc -l` over the published crate's `.c`,
`.h` and `.S`/`.s` files for the first two, and over its `.rs` files for
the other two. ADR-0002's precedent applies to code small enough to own,
which BLAKE-256 was and none of these are.

## Maintaining this

Add a row when a dependency becomes consensus-observable or reaches key
material. Change a row's decision when someone actually reads the crate —
`reviewed@V` is a claim about a person having done that, and should not be
written otherwise.
